{
  "name": "fps",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "smoke:full-clear": "node scripts/full-clear-smoke.mjs --fresh-session",
    "smoke:mechanics": "node scripts/mechanics-smoke.mjs --fresh-session",
    "smoke:skybox-readback": "node scripts/skybox-readback-smoke.mjs --fresh-session",
    "aperture": "aperture"
  },
  "dependencies": {
    "@aperture-engine/app": "workspace:*",
    "@aperture-engine/audio": "workspace:*",
    "@aperture-engine/physics": "workspace:*",
    "@aperture-engine/physics-rapier": "workspace:*",
    "@aperture-engine/render": "workspace:*",
    "@aperture-engine/simulation": "workspace:*",
    "@aperture-engine/vite-plugin": "workspace:*",
    "@aperture-engine/webgpu": "workspace:*"
  },
  "devDependencies": {
    "@aperture-engine/cli": "workspace:*",
    "typescript": "^6.0.3",
    "vite": "^8.0.13"
  }
}

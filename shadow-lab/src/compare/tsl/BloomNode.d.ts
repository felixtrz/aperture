export function bloom(
  inputNode: unknown,
  strength?: number,
  radius?: number,
  threshold?: number,
): any;

export default class BloomNode {
  constructor(
    inputNode: unknown,
    strength?: number,
    radius?: number,
    threshold?: number,
  );
}

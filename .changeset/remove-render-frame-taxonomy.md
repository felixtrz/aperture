---
"@aperture-engine/render": patch
"@aperture-engine/webgpu": patch
---

Remove the orphan render-package frame-phase helper exports and keep the
canonical submitted-frame phase descriptors in `@aperture-engine/webgpu`.

---
"@aperture-engine/app": patch
"@aperture-engine/cli": patch
---

Fix the headless validation and render tooling issues found in the packed-engine battle test: reliable blank-frame detection, headed render capture on Linux, physics parity from shared config, deterministic serve enforcement, stricter ECS/devtools selectors, session snapshot/restore, command dispatch, seed reporting, and clearer diagnostics for asset, system, and spawn-shape failures.

---
"@aperture-engine/physics": patch
"@aperture-engine/render": patch
"@aperture-engine/simulation": patch
---

Remove test-only simulation helpers and de-export internal render and physics validation helpers while preserving live public alternatives.

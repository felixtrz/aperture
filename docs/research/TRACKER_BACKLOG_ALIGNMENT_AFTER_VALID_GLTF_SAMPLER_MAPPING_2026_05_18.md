# Tracker Backlog Alignment After Valid glTF Sampler Mapping

Date: 2026-05-18

Task: `task-1563`

## Scope

Align public tracker pages and the ready backlog after valid non-default glTF
sampler mapping browser coverage.

## Updates

- Updated `docs/index.html` to summarize the valid sampler mapping fixture and
  point next focus at follow-up planning.
- Updated `docs/render-pipeline-comparison.html` to include valid non-default
  glTF sampler enum mapping in the prepare phase status.
- Updated `agent/BACKLOG.md` to mark `task-1561` and `task-1562` complete and
  queue planning/audit/tracker follow-ups.

## Ready Queue Check

At least five categorized, scoped ready tasks remain:

1. `task-1564` — plan next route or StandardMaterial follow-up after valid
   sampler mapping coverage.
2. `task-1565` — audit selected follow-up plan after valid sampler mapping
   planning.
3. `task-1566` — audit tracker/backlog alignment after valid sampler follow-up
   plan audit.
4. Add or select the implementation task chosen by `task-1564`.
5. Keep a focused audit-refactor task after the next implementation slice.

## Validation

Run `pnpm run check:progress` after formatting.

# Framework-Comparison Gap Verification & Action Items

**Status:** active backlog · **re-verified 2026-06-08** against current `gap-fixes-batch-1` source — see the 2026-06-08 Implementation Log entry, the refreshed §1/§2 status, and the re-prioritized §7 sequencing
**Created:** 2026-06-07
**Source:** Verification of `references/framework-comparison-research/` (the 121-gap Aperture report, a 2026-06-06 point-in-time snapshot) against current source, via a 31-agent verify → adversarial-recheck → synthesize workflow on Claude Opus 4.8.
**Method:** Every gap re-checked against today’s code and classified (still-open / partial / outdated-fixed / wrong / by-design); each gap cross-referenced against `docs/CODE_AUDIT_REMEDIATION_PLAN.md`; status-flips and all critical/high verdicts adversarially rechecked. The top P1 action items and every “now-closed” closure were additionally spot-checked by hand.
**Spot-check note:** AI-71’s “4 of 145 specs” gating lives in `.github/workflows/ci.yml` (not `playwright.ci.config.ts`, which only ignores `dof.spec.ts`) — reconfirm the exact count there when picking it up; the direction (expand e2e coverage + real-GPU matrix) holds regardless.

> This is an automated, point-in-time verification. Spot-check anything load-bearing before committing significant work to it. AI-N numbers are stable identifiers for the action items.

---

## Implementation Log

### 2026-06-07 — batch 1 (branch `gap-fixes-batch-1`)

Implemented, each covered by tests, full `pnpm run check` green:

- **CI unblock (pre-existing):** removed two dead-symbol lint errors that were already failing `pnpm run check` on the tip (leftovers from the prior remediation wave): a dead `jsonSafeValue` import in `packages/app/src/entities/lookup/summary.ts` (DUP-02 residue) and an unused `aperture` binding in `examples/render-to-texture.main.js` (DC-01 residue).
- **AI-79 — defuse the `check:progress` freshness time-bomb.** `scripts/check-progress-tracker.mjs` now treats docs date-staleness as a **non-fatal warning** by default (structural phase-board + missing/future-date checks stay fatal); staleness is promoted back to a failure only under `APERTURE_PROGRESS_TRACKER_ENFORCE_FRESHNESS=1`. Logic extracted to a pure `evaluateProgressTracker()` behind a main-guard. Tests: `test/scripts/check-progress-tracker.test.mjs` (6).
- **AI-63 — bound the change-detection map on entity destroy.** `packages/simulation/src/ecs/index.ts` `patchedDestroy` now deletes the destroyed entity's `versionByEntityKey` entry (captured before destroy), so the map tracks the live set instead of leaking one key per spawn/despawn cycle. Added a diagnostic `entityVersionTrackingSize()` accessor. Tests: `test/ecs/entity-version.test.ts` (+2: churn-bound + key-drop).
- **AI-48 — reject non-primary pointer bindings.** `packages/app/src/config/validation.ts` `validatePointerName` now throws a clear `aperture.config.invalidInputBinding` for `pointer: 'secondary' | 'middle'` (which the forwarder never emits and the input state never models, so they silently no-op) instead of accepting a dead binding. Tests: `test/app/input-pointer-binding-validation.test.ts` (4).

Deferred after investigation (regression risk vs the stated one-liner):

- **AI-28 (MSAA widen to 2|8):** WebGPU core only permits `sampleCount` 1 or 4 — widening would emit invalid configs that throw at texture creation. Needs a capability-query design, not a constant widen.
- **AI-1 (primitive-collider scale diagnostic):** the Rapier recreation signature is `colliderKeyFor = JSON.stringify(colliders)`, so adding `scale` to primitive descriptors (required to reach the diagnostic) would recreate the collider every frame for animated-scale entities — a perf regression. Needs the signature to exclude scale first.

### 2026-06-07 — batch 2 (branch `gap-fixes-batch-1`)

- **AI-9 — O(1) collision-event collider-handle lookup.** `events.ts` resolved every contact pair via a nested O(bodies×colliders) `colliderMatchForHandle` scan (up to 4× per pair, per step). Added `buildColliderHandleIndex(bodies)` (`packages/physics-rapier/src/colliders.ts`), built **once per step** in `collectRapierEvents` and looked up O(1) in `eventPairForColliderHandles` / `contactEventData` / `contactForcePhysicsEvent`. Rebuilt each step from the live body store, so it can never go stale across upsert/destroy (no manual lifecycle bookkeeping). Typecheck caught a missed stay-event call site that would otherwise have silently broken stay-event contact data. Tests: `test/physics-rapier/collider-handle-index.test.ts` (3, incl. parity with the scan); existing 120 physics tests green.
- **AI-25 — FrameGraph route via config, not just a URL flag.** Added `render.frameGraph` to `ApertureRenderDefaults` and a pure `resolveUseFrameGraph(render, search)` (`packages/app/src/browser/frame-graph-route.ts`) used by the browser harness; the legacy `?graph=1` flag still works as a per-load override. Tests: `test/app/frame-graph-route.test.ts` (4). (CI-gating of the graph route + default-flip remain follow-ups.)

Deferred this round:

- **AI-30 (extraction scratch buffers):** reuse-across-frames aliases with the extraction cache and transfer/SAB paths; safer as a benchmarked perf pass (like FEAT-03), not an unscoped hoist.
- **AI-82 (RAG "embedding"→"lexical"):** the honest version renames the on-disk index format/model contract (breaking) or is docs-only (untestable); not a clean tested win.
- **AI-86 (portable doc paths):** mixes in-repo `references/`, external libs needing upstream URLs, and a separate private project (`immersive-web-sdk`) with no public equivalent — needs per-doc curation/input.

### 2026-06-07 — batch 3 (branch `gap-fixes-batch-1`)

- **AI-5 (partial) — surface BVH closest-point through the spatial facade.** The gameplay `SpatialQueries` facade exposed only raycasts; `MeshBvh.closestPointToPoint` existed but was unreachable. Added `closestPoint(point, options?)` (`packages/app/src/spatial/closest.ts`, wired in `spatial/index.ts`) that transforms the world point into each BVH-backed mesh's local space, resolves the closest surface point, transforms it back, and picks the global nearest by world distance (with a world-space `maxDistance` filter and entity/layer filtering). Exported `meshQueryTransforms` for reuse. Tests: `test/app/spatial-closest-point.test.ts` (6, analytically verified against a known plane: interior projection, edge-clamp, world-from-mesh transform, maxDistance, no-BVH skip).

### 2026-06-07 — batch 4 (branch `gap-fixes-batch-1`)

- **AI-5 (cont.) — `overlapSphere` on the spatial facade.** Added `overlapSphere(center, radius, options?)` (`packages/app/src/spatial/overlap.ts`) returning the entities whose BVH-backed mesh overlaps a world-space sphere, via `MeshBvh.intersectsSphere` with the center transformed into mesh space (radius world-space, exact for rigid transforms). Tests: `test/app/spatial-overlap-sphere.test.ts` (6, analytically verified: surface-reaching sphere, near-miss, radius-bridges-edge, world-from-mesh transform, no-BVH skip).

### 2026-06-07 — batch 5 (branch `gap-fixes-batch-1`)

- **AI-5 (cont.) — complete the `overlapShape` family: `overlapBox` + `overlapCapsule`.** Refactored `overlap.ts` to a shared `collectOverlaps` core and added box (via `MeshBvh.intersectsBox` with the `boxToMesh` transform — localized exactly including rotation) and capsule (via `intersectsCapsule` with endpoints transformed into mesh space) overlap queries. The facade now exposes `overlapSphere`/`overlapBox`/`overlapCapsule`. Tests: `test/app/spatial-overlap-shapes.test.ts` (8, analytically verified: box straddle/above/outside, capsule segment-cross/radius-reach/clear, no-BVH skip). **AI-5 `overlapShape` is now complete**; the remaining piece is shapecast/`castShape` (sweep) on top of `bvhcast`, deferred as a focused follow-on (swept-volume math).

### 2026-06-07 — batch 6 (branch `gap-fixes-batch-1`)

- **AI-31 — reusable fly / first-person camera controller.** Only an orbit controller was barrel-exported; added `createFlyCameraController` (`packages/app/src/controllers/fly-camera.ts`, exported from the controllers barrel → `@aperture-engine/app`), mirroring the orbit controller's ECS-authoritative, input-agnostic, headless-safe design. Holds eye position + yaw/pitch; `look`/`lookFromDrag` map pointer drag to orientation (pitch clamped inside the poles), `move(forward, right, up)` translates along the pitched view basis, and `applyTo(world)` writes the camera `LocalTransform` via the component path. Tests: `test/app/fly-camera-controller.test.ts` (7: drag→yaw/pitch, pole clamp, forward/right basis, view-basis move, pitched-fly altitude gain, real-world `applyTo`, stale-ref → false). A runnable e2e example is a nice-to-have follow-on (the orbit controller's e2e covers the harness pattern).

### 2026-06-07 — batch 7 (branch `gap-fixes-batch-1`)

- **AI-34 (reader half) — apply KHR sparse accessor overrides.** `decodeGltfFloatAccessor` (the shared skin/animation float-accessor reader) silently returned zero-filled values for sparse accessors. Restructured it to read the base values (optional bufferView; zeros if absent) and then apply a `applyGltfSparseOverride` pass that reads the `sparse.indices`/`sparse.values` bufferViews and overwrites the addressed elements; malformed/out-of-range sparse data fails the decode. Files: `packages/render/src/assets/gltf-accessor-float-reader.ts`. Tests: `test/assets/gltf-accessor-sparse.test.ts` (4, hand-constructed bytes: base+override, pure-sparse, absent-bufferView-no-sparse still zero-fills, out-of-range→null). Follow-on: the _primitive-attribute_ validator still emits `gltfAccessor.sparseAccessorDeferred` for vertex attributes (a separate path); upgrading that to apply sparse on vertex attributes is the remaining AI-34 slice.

### 2026-06-08 — re-verification pass (no code change)

Re-verified all 86 numbered AI-N items (plus the §5 by-design set) against current `gap-fixes-batch-1` source via a 26-agent verify → reconcile → synthesize workflow on Claude Opus 4.8 (~1.29M tokens, 462 tool calls). Every cited `file:line` was re-opened in today's tree; the 121→86 dedup was re-checked and is intact with no orphan. **No code changed in this pass** — it refreshes statuses and the do-next plan only.

**Status flips since the 2026-06-07 batches:**

- **Done (5):** AI-9 (O(1) collision-handle index, `da7c2833`), AI-31 (fly/FPS camera controller, `7d5e035c`), AI-48 (reject secondary/middle pointer bindings — validation slice, `76e77727`), AI-63 (version-map key delete in `patchedDestroy`, `76e77727`), AI-79 (`check:progress` staleness made non-fatal, `76e77727`).
- **Partial (5):** AI-5 (overlapSphere/Box/Capsule + closestPoint landed; swept `castShape*` + `explainReadiness()` left), AI-25 (route now config-driven; default-flip + CI graph-route gate left), AI-34 (float reader applies sparse; primitive vertex-attribute path left), AI-41 (all-N morph import already shipped; TANGENT deltas left), AI-87 (PMREM real; no-source placeholder residue — **now promoted into §3**).
- **Wrong (1):** AI-3 — the parent-local↔world pose conversion already exists (`localTransformFromPhysicsResult` inverts the parent matrix on writeback at `ecs-sync.ts:369-422`; `physicsTransformForEntity:510-540` composes a world pose on send). The `parented:true` rejection only fires when a parented entity has **no** `WorldTransform`. Reclassified to a small P3/S "guarantee WorldTransform propagation before physics sync, then drop the residual rejection" task.
- **By-design (17) and the §4 closures (8):** re-checked against current source — **all still hold**. The §6 corrections also still hold (notably the "full e2e only via SessionStart hook" finding remains **WRONG** — `playwright.config.ts` is git-committed since 2026-05-15 and runs the whole `test/e2e` dir under headed real Chrome).

**Themes in the remaining work** (each names the AI-N it spans):

- **GPU-driven scale & dirty-tracking** — sub-range buffer-upload primitive and its consumers (AI-66, AI-64, AI-65), persistent extraction cache + scratch reuse (AI-13, AI-30, AI-67), incremental transform/spatial scheduling at the root (AI-60), GPU-driven indirect culling + LOD (AI-21, AI-22, AI-24), and spatial broad-phase for both render culling and the query facade (AI-6, AI-5, AI-10).
- **Rendering completeness & color/output contract** — normal G-buffer + live view uniform unblocking general SSR (AI-19, AI-20), forward/legacy paths through the frame graph with user passes (AI-12, AI-25, AI-23), shared output tonemap/encode + wide-gamut/HDR (AI-17, AI-29), and the lighting/GI frontier (AI-14, AI-18, AI-26, AI-27, AI-87).
- **Physics fidelity & honest capabilities** — primitive-collider scale (AI-1), test-backend solver vs. honest flags (AI-4), joint impulse readback / break-force / generic-joint design (AI-8, AI-7), world-COM lever arms (AI-2), and query-shape cooking (AI-10).
- **Platform input / text / UI surface** — wheel transport + UiScroll (AI-47), multi-pointer model + per-pointer picking (AI-44, AI-51), gamepad haptics / pointer-lock / remap (AI-49), chord/timing bindings (AI-50), IME/editable text + clipboard (AI-45, AI-54), UI focus & keyboard/gamepad nav (AI-46), flex/grid layout + text shaping (AI-53, AI-52).
- **Determinism, scheduling & worker transport** — fixed-rate sim clock + frame-stamped snapshots + input ring (AI-55, AI-56, AI-61), richer scheduler with named phases / plugins / parallelism (AI-57, AI-58, AI-59, AI-68), SAB transport + COOP/COEP plumbing (AI-69, AI-70).
- **glTF authoring/import breadth** — lights/cameras import (AI-32, AI-33), sparse-accessor primitive path (AI-34), PBR-next extensions (AI-38), non-triangle primitives + extra attribute slots (AI-39, AI-40), morph tangents (AI-41), GPU instancing (AI-42), CORS-aware URI resolution (AI-43).
- **CI rigor & external-adoption gates** — sharded real-GPU e2e matrix (AI-71), golden-image baselines (AI-74), skip-count gating (AI-75), coverage thresholds (AI-77), security tooling (AI-78), perf benchmarks (AI-76), the first real release + changelog (AI-72), docs/API site + framework bindings (AI-80, AI-81, AI-83, AI-84).

See §7 for the re-prioritized do-next sequence.

### 2026-06-08 — batch 8 (branch `gap-fixes-batch-1`)

- **AI-1 — apply non-unit scale to primitive colliders + diagnose inexact scale.** Primitive colliders (box/sphere/capsule/cylinder/cone) on a scale≠1 entity silently kept unit size, desyncing the collider from the scaled render geometry. Grounded the fix in the PlayCanvas reference (`references/engine/.../collision/system.js`), which applies the entity world scale to its collision shapes and recreates only on scale change. New `packages/physics/src/collider-scale.ts` (`scaleColliderShape`, `isColliderScaleApproximated`, `isNonUnitScale`); `createColliderDescriptor` (`ecs-sync.ts`) now bakes the collider entity's scale into primitive shape dims (box exact per-axis; sphere/capsule/cylinder/cone use an enclosing largest-axis factor) and carries `scale` for non-unit primitives. `collectUnsupportedPhysicsBodyFeatures` (`backend.ts`) emits a new `physics.collider.scale.approximated` diagnostic when a non-uniform scale can't be represented exactly (rather than silently approximating). Unit-scale primitive descriptors are unchanged (no per-frame churn; recreate-on-scale-change matches PlayCanvas). Tests: `test/physics/collider-scale.test.ts` (17: baking per shape/axis, diagnostics, pure-helper math). Full `typecheck`/`typecheck:test`/`lint`/`format` green; 184 physics tests pass. Residual: local-scale (consistent with the asset path) — world-scale for nested colliders pairs with AI-3.
- **AI-82 (docs half) — stop advertising the Reference RAG as "embedding-backed."** It is a FNV-1a hashed bag-of-words compared by cosine similarity, not a semantic embedding. `docs/AI_TOOLING.md:54` now describes it honestly as lexical (hashed bag-of-words) keyword search and points at the `aperture-reference-hash-embedding` contract. The real-embedding-model swap (breaking on-disk index contract) remains the deferred residual.
- **AI-70 (pt.1) — only post source-assets when changed.** `publishGeneratedWorkerSnapshot` (`worker/snapshot.ts`) now attaches the `sourceAssets` field only when the version-gated registry produced a changed entry; steady-state frames omit it and the receiver (`mirrorSourceAssetRegistryFromMessage`) no-ops on an absent/empty field. Drops a per-frame postMessage payload on both transport paths. Tests: `test/app/asset-mirror.test.ts` (+1, the no-op receiver contract). Residual (pt.2): shared-packet encoders for the unsupported families.
- **AI-28 — data-driven MSAA sample-count selection (premise corrected).** WebGPU core only guarantees sampleCount 1 and 4, so "widen to 2|8" is infeasible. `resolveWebGpuMsaaConfig` (`gpu/msaa.ts`) now derives the resolved count from `WEBGPU_SUPPORTED_MSAA_SAMPLE_COUNTS` via `nearestSupportedSampleCount` (round up to next supported, cap at max) instead of a hardcoded `>1?4:1`; behavior unchanged for `[1,4]`, future widening is a one-line edit, and texture-creation validation remains the runtime device check. Tests: `test/webgpu/msaa.test.ts` (+1).
- **AI-86 (most) — portable doc paths.** Converted 183/269 machine-specific `/Users/felixz` citations across the planning/research docs to portable paths (repo-own → `./`-relative; `aperture-reference-libs/three.js` → `references/three.js`; `.../playcanvas-engine` → `references/engine`) and re-aligned SOTA_ROADMAP tables. Residual (external libs not cloned + private `immersive-web-sdk`) needs upstream URLs — mapping recorded on the AI-86 item.
- **AI-30 — sequenced behind AI-76.** Left unimplemented by design: FEAT-03 showed naive buffer reuse ~3.9× slower, so this is now an explicit measure-then-hoist item gated on the AI-76 benchmark harness (recorded on the AI-30 item) rather than a blind change.

### 2026-06-09 — batch 9 (branch `claude/nice-franklin-5sray8`)

- **AI-11 — gate the per-frame GPU drain on a real readback need.** All five frame paths (`frame-loop.ts`, `sprite-frame.ts`, `custom-wgsl-frame.ts`, `mixed-custom-wgsl-frame.ts`, `queued-built-in-frame.ts`) awaited `waitForSubmittedWork` (`queue.onSubmittedWorkDone`, a full GPU flush) unconditionally every frame. Added a pure `frameBoundariesNeedGpuDrain(boundaries)` predicate (`app/report.ts`) that is true only when the assembled frame produced something a CPU read actually consumes this frame — a requested readback boundary, pending GPU-timing readbacks, or occlusion-query readbacks/queries — and gated all five drains on it. `picking-frame.ts:342` left as-is per the item. Readback-carrying frames (e2e canvas probes, occlusion feedback, GPU timings) keep the exact prior ordering. Tests: `test/webgpu/frame-boundary-drain.test.ts` (5); full webgpu suite (1404) green. Residual: consuming occlusion readbacks N frames later (latency hiding) remains a follow-on.
- **AI-3 — guarantee parented rigid bodies resolve a world pose; rejection now fires only for genuinely unresolvable poses.** A parented `RigidBody` authored with only a `LocalTransform` was flagged `parented:true` and dropped from simulation, even though both pose conversions already existed. New `ensureParentedPhysicsBodyWorldTransforms(world)` (`ecs-sync.ts`) adds `WorldTransform` to parented bodies before transform resolution; `stepPhysicsWorld` and the worker-transfer `stepWorld` both run it ahead of `resolveWorldTransforms`, so the body syncs at the composed world pose and writeback round-trips to parent-local. The `parented:true` flag (and `physics.rigidBody.parentedBody.unsupported`) now only fires when the world pose is genuinely unresolvable (raw `collectPhysicsCommands` without a resolve pass, or a degenerate/non-decomposable matrix) — message/suggestedFix updated to say so (invariant 5: loud, not silent). Tests: `test/physics/test-backend.test.ts` (+1: translated+rotated parent, body authored without WorldTransform → synced at world pose, writeback preserves parent-local translation, no parented flag on re-collect); existing parented/unsupported tests unchanged and green (144 physics tests).

### 2026-06-09 — batch 10 (branch `claude/nice-franklin-5sray8`) — readiness roadmap Phase R1

Executes Phase R1 of `docs/AGENT_FIRST_READINESS_ROADMAP.md` (verification rigor) plus regression fixes:

- **AF-1 — ten weakest-covered modules lifted to 96–100% statements** (340 new tests): shadow-depth-probe 6.3%→96.4%, frame-boundary 43→100, reference/source-collection 44→100, entities/lookup/mutation 51→100, id-buffer-pick 51→100, app/frame-boundaries 58→99, physics/worker-transfer 65→100, config/validation 66→100, runtime/simulation-worker 66→100, input/state 67→99. Workspace coverage 82.99%→85.45% statements, 71.19%→73.91% branches. Two latent issues found and fixed in follow-up: the PhysicsGravity missing-component diagnostic now uses the family code `aperture.entityLookup.componentMissing`, and frames that gracefully fall back from unsupported occlusion query sets stay `valid:true` (warning diagnostic + culling fallback remain the loud signal).
- **AI-77 — coverage thresholds gate CI.** `vitest.config.ts` thresholds at 85/73.5/90/85 (just below measured reality, ratchet-up-only documented); dedicated parallel CI job runs `pnpm run test:coverage`.
- **AI-71 (sharding half) — full e2e suite in CI.** The 4-spec hand-picked list is replaced by an 8-way shard matrix over all of `test/e2e` (61 tests/shard, `fullyParallel` for even splits, workers:1 serial within a shard); `playwright.ci.config.ts` `testIgnore` is the single documented home for environment exclusions (dof, clustered-lights, csm-directional-shadow under SwiftShader). Real-GPU OS matrix remains the open half.
- **AI-75 — unexpected-skip gating.** `scripts/check-e2e-skips.mjs` (pure core + unit tests) fails a shard when a skipped test's reason doesn't match a documented environment-capability pattern; wired via the new JSON reporter output.
- **AI-74 — seven golden-image baselines** over frame-stable routes (auto-shadow, multi-light-shadow, area-light-shapes, ibl-irradiance, standard-gltf-texture, msdf-text, ui-interaction) at a 1500-pixel diff budget; generation + re-verify runs green under the CI SwiftShader config; refresh flow documented in `docs/RENDER_CONTROL.md`.
- **AI-76 — bench harness + CI frame budget.** `pnpm run bench` reports per-scale (10/100/1k/10k) timings for extract / pack / cull / draw-packages / encode plus the transform-upload path; the gating half (`extraction-budget.test.ts`) runs under `pnpm test` asserting exact snapshot counts, a documented generous 250ms median budget, and scene-size scaling. Unblocks the R5 measure-then-change items (AI-13/AI-30/AI-64).
- **Regression fix — root `elics` restore + import-map guard.** Batch 1's dead-dependency removal broke all 123 example pages (import maps resolve `elics` from the workspace root — invisible to static analysis). Restored with `knip` `ignoreDependencies` documenting the browser-served-asset role, and `check-examples.mjs` now resolves every example import-map target through the server's own path resolution so this class of breakage fails `pnpm run check` instead of only e2e.

---

## 1. Executive Summary

This audit verified **121 framework-comparison gaps** against current `aperture` source, each adversarially rechecked. The headline: a meaningful slice of the original critical/high backlog has genuinely landed, the largest remaining blockers are concentrated in **GPU-driven scale, determinism, platform (input/text/UI), and CI rigor**, and a clearly-bounded set of platform features (audio, networking, a11y, i18n) are real-but-deliberate non-goals.

**Verification outcome (by verdict):**

- **still-open: 66** — genuinely unfixed and actionable.
- **partial: 8** — half-landed; a residual slice remains.
- **outdated-fixed: 8** — closed by recent remediation since the snapshot.
- **by-design: 17** — real gaps, intentionally out of scope (no action item).
- **wrong: 1** — the original gap claim was factually incorrect.

> **Re-verified 2026-06-08** (the 86 numbered AI-N items, against current `gap-fixes-batch-1` source): **done 5** (AI-9, AI-31, AI-48, AI-63, AI-79 — all landed since 2026-06-07) · **partial 5** (AI-5, AI-25, AI-34, AI-41, AI-87) · **wrong 1** (AI-3 — conversion already implemented; reclassified P3/S) · **open 75** (incl. the deferred-with-blocker set AI-1/AI-28/AI-30/AI-70/AI-82/AI-86). The 8 §4 closures and 17 §5 by-design verdicts were re-checked against current source and **all still hold**; the 121→86 dedup is intact with no orphan. AI-87 (an IBL residue that previously lived only in §6) is now promoted into the numbered §3 list.
>
> **Batch 8 update (2026-06-08, same day):** the deferred-with-blocker set was then worked through so every item is cleanly actionable — **AI-1** (primitive-collider scale) and **AI-28** (MSAA, premise corrected) resolved; **AI-70 pt.1** and **AI-82** (docs) landed (each now ◐ with a clearly-scoped residual); **AI-86** mostly done (183/269 paths, residual = upstream URLs); **AI-30** sequenced behind the AI-76 benchmark (must not be implemented blind — FEAT-03 showed naive buffer reuse regresses). See the batch-8 Implementation Log entry above.

**What recently got fixed (themes):** the recent remediation wave (FEAT-/PHY-/DC-/DOC- tickets, mostly dated 2026-06-06/07) closed a coherent cluster:

- **Asset-backed colliders** — convexHull/trimesh/heightfield now actually cooked from provider geometry (PHY-05/PHY-02).
- **SharedArrayBuffer SeqLock transport** — write side now driven by the _default_ generated worker, not just `examples/sab-cube` (FEAT-02). Two separate gap entries reporting this both verified closed.
- **IBL** — equirect→cube is wired on-GPU into the asset pipeline, specular PMREM/GGX prefilter is real and engaged, diffuse cosine-irradiance convolution real (FEAT-04/FEAT-06, M5-T2).
- **Transfer-list snapshot transport + buffer-pool decision** — transfer list now passed; the dead buffer-pool surface was deliberately _deleted_ after a benchmark showed it ~3.9x slower (FEAT-03).
- **Particle over-lifetime curves** (size/color LUT) shipped (FEAT-01) — though the simulation itself remains a hash re-scatter (still partial).
- **Tooling hygiene** — `check:examples` de-hardcoded to a dynamic enumerator (DOC-01); MCP/devtools docs/decisions added (DOC-05/06).

**Biggest still-open blockers (surviving critical/high):**

- **Audio (critical)** — entirely absent; classified by-design (out of North Star scope).
- **CI runs only 4 of 145 e2e specs** on SwiftShader, single OS/Node (AI-19, P1) — ~97% of e2e proofs are non-gating.
- **All packages unpublished** to npm (AI-20, P1) — engine is un-adoptable externally.
- **No golden-image visual regression** (AI-33, P2) — color/gamma/shading drift is undetectable.
- **Unconditional per-frame GPU drain** defeats CPU/GPU pipelining (AI-11, P1).
- **Single-pointer-only input**, no IME/editable text, no UI focus/keyboard nav, no wheel transport (AI-21/AI-43/AI-44/AI-45) — platform blockers for real products.
- **Flat scheduler, no parallelism, free-running wall-clock sim loop** — architecture ceiling for determinism and many-entity scale.

**By-design set (real but out-of-scope):** audio, networking/replication, accessibility, i18n, FBX/OBJ/USD/PLY/VRM/gsplat import, WebP/AVIF textures, mesh LOD (M8-T8), deferred/visibility-buffer path (M8-T9), the analytic-DFG BRDF LUT decision, the `/testing`-gated test backend, the dedicated physics-worker route, the on-screen stats overlay (M11-T1), and WebXR (Phase 14). Each is acknowledged below; several would need a **roadmap decision** to pick up.

---

## 2. Status Ledger

**Counts by verdict × severity:**

| Verdict        | critical | high   | medium | low    | **Total** |
| -------------- | -------- | ------ | ------ | ------ | --------- |
| still-open     | 0        | 16     | 33     | 17     | **66**    |
| partial        | 0        | 3      | 3      | 2      | **8**     |
| outdated-fixed | 1        | 3      | 2      | 2      | **8**     |
| by-design      | 1        | 3      | 6      | 7      | **17**    |
| wrong          | 0        | 0      | 1      | 0      | **1**     |
| **Total**      | **2**    | **25** | **45** | **28** | **100**   |

> Note: the dataset contains several deliberate duplicate entries (e.g. two SAB-write-side gaps, two networking gaps, three frame-graph-aliasing gaps, multiple on-disk-scene-format and dirty-range-upload gaps). The 100 rows above reflect the JSON as supplied; action items below dedupe them into single tickets where they describe the same work.

**Actionable items (still-open + partial only): tracking status**

|                                                                                                                                                          | Count |
| -------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- |
| Action items already tracked in `CODE_AUDIT_REMEDIATION_PLAN.md`                                                                                         | **0** |
| Action items tracked elsewhere (SOTA*ROADMAP / NORTH_STAR / MEDIUM_LONG_TERM_GOALS / inline `audit Bx`/`L15` notes / impl plans / OPTIMIZATIONS*\* docs) | ~28   |
| **NEW** (no tracking of any kind)                                                                                                                        | ~30   |

**2026-06-08 re-verification delta:** of the still-open + partial actionable set, **5 are now done** (AI-9 / AI-31 / AI-48 / AI-63 / AI-79) and **1 is wrong / largely-implemented** (AI-3), leaving ~70 open + 5 partial actionable AI-N (AI-87 added to the partial set; see the 2026-06-08 Implementation Log entry). No item is tracked by an active `CODE_AUDIT_REMEDIATION_PLAN.md` ticket; the done/partial ones are recorded in the Implementation Log above.

Key finding: **none of the open/partial action items are tracked by an active `CODE_AUDIT_REMEDIATION_PLAN.md` ticket** — that plan's open work has all landed. Every remaining item is either a roadmap/milestone aspiration (SOTA M8/M9/M11, NORTH_STAR goals), an inline "tracked follow-up" comment, or completely untracked.

---

## 3. Action Items

Open and partial gaps. Grouped by theme; ordered by priority within each group. AI numbers are stable across the whole document. Items completed or reclassified in the 2026-06-08 re-verification are **retained here with a trailing status flag** (✅ done / ◐ partial / ✗ wrong) so the stable AI-N IDs and their evidence stay in one place.

### Physics & Spatial

- **AI-1** · **Apply or diagnose non-unit scale for primitive colliders** · **P1** · **M** · Primitive colliders (box/sphere/capsule/cylinder/cone) on a scale≠1 entity silently keep unit size with **no diagnostic**; only asset-backed shapes get the `physics.collider.scale.unsupported` warning. Either apply LocalTransform/world scale in `readColliderShape`/`createColliderDescriptor`, or extend the diagnostic to primitives so the collider/render desync is surfaced. · Files: `packages/physics/src/ecs-sync.ts:803-856,613-647`; `packages/physics/src/backend.ts:558-591` · **✅ DONE — gap-fixes-batch-1 (batch 8, 2026-06-08)** — primitive scale is now baked into shape dims (`collider-scale.ts` `scaleColliderShape`, applied in `createColliderDescriptor`), matching how PlayCanvas applies world scale to collision shapes; a `physics.collider.scale.approximated` diagnostic fires when a non-uniform scale can't be represented exactly (sphere / capsule radial). Recreate-on-scale-change via `colliderKeyFor` is PlayCanvas-equivalent (no per-frame thrash for static scale). Residual follow-up: uses the collider entity's **local** scale (consistent with the asset path); composing **world** scale for nested colliders pairs with AI-3.

- **AI-2** · **Compute `point` lever arm from world COM and world transform** · **P2** · **M** · `applyForce`/`applyImpulse` off-center torque uses `LocalTransform.translation` as the center, ignoring the body's real COM and parent transform (local translation is parent-relative while point/force are world-space). Read the backend world COM (and world transform for parented bodies) and compute `cross(worldPoint − worldCom, force)`. · Files: `packages/app/src/systems/physics.ts:783-799`; physics backend body accessor · **NEW**

- **AI-3** · **Implement parent-local ↔ backend-world pose conversion for parented rigid bodies** · **P2** · **L** · Parented dynamic bodies that resolve only a local transform are flagged `parented:true` and **dropped from simulation** (`physics.rigidBody.parentedBody.unsupported`). Compose the parent world matrix to send a world pose on sync; convert back to parent-local on writeback; remove the rejection. · Files: `packages/physics/src/ecs-sync.ts` (`physicsTransformForEntity:510-540`, `createUpsertBodyCommand:465-467`); `packages/physics/src/backend.ts:545-554,725-728` · **✗ WRONG (re-verified 2026-06-08)** — the world↔parent-local conversion already exists: `localTransformFromPhysicsResult` inverts the parent matrix on writeback (`ecs-sync.ts:369-422`) and `physicsTransformForEntity:510-540` composes a world pose on send. The `parented:true` rejection only fires when a parented entity has **no** `WorldTransform`. Reclassified to **P3/S**: guarantee WorldTransform propagation before physics sync, then drop the residual rejection. **✅ DONE (batch 9, 2026-06-09)** — `ensureParentedPhysicsBodyWorldTransforms` runs before transform resolution in `stepPhysicsWorld` and the worker-transfer step path; the rejection remains only for genuinely unresolvable world poses (loud, per invariant 5).

- **AI-4** · **Add minimal contact/joint resolution to the test backend — or flip the capability flags honest** · **P2** · **L** · The deterministic test backend's `step()` integrates only kinematics/forces/gravity/damping; dynamic bodies interpenetrate statics/ground, joints apply no constraint — yet it advertises `compoundColliders:true` and `characterController:true`. Add sphere-sphere/sphere-plane contacts + a basic joint solver, or set those flags `false` and document it integrate/query-only. · Files: `packages/physics/src/test-backend/backend.ts:241-303`, `.../bodies.ts`; `packages/physics/src/backend.ts:39-42` · **already tracked: DC-14 / plan line 832 (gating only — does not bless integrate-only behavior)**

- **AI-5** · **Wire MeshBvh overlap/closest-point/shapecast into the SpatialQueries facade** · **P2** · **L** · The gameplay spatial facade exposes only `raycastFirst/raycastAll/setBounds/setMeshes`; `MeshBvh.intersectsSphere/Box/Capsule`, `closestPointToPoint`, and `bvhcast` exist but are unsurfaced. Add `overlapShape(All)`, `castShapeFirst`, `closestPoint`, `explainReadiness`. · Files: `packages/app/src/spatial/types.ts:71-82`, `.../index.ts:42-108`; `packages/simulation/src/spatial/mesh-bvh.ts` · **◐ PARTIAL — gap-fixes-batch-1 (`265d6bf7`/`f07ba5e2`/`34257fd6`); re-verified 2026-06-08** — `closestPoint` + `overlapSphere`/`overlapBox`/`overlapCapsule` landed (overlapShape family complete). Residual: swept `castShape*` (via `bvhcast`) and `explainReadiness()`.

- **AI-6** · **Wire EntityBoundsBvh as a membership-diffed broadphase for multi-pick / large scenes** · **P2** · **L** · The world broadphase BVH is built and exported but never used; the default picker is a linear O(n) bounds scan. Add membership-diffed refit and wire it into `createSpatialQueries` for `raycastAll`/large-scene `raycastFirst`, keeping linear scan only for tiny counts. · Files: `packages/simulation/src/spatial/entity-bounds-bvh.ts`; `packages/app/src/spatial/index.ts` · **NEW (inline PERF NOTE only)**

- **AI-7** · **Design backend-neutral generic joint + true min/max distance joint, or document permanent exclusion** · **P2** · **L** · `kind:'generic'` is accepted at the authoring boundary then **throws** at the Rapier backend; `distance` maps to a one-sided rope (no min/max). Add a generic constraint axis/mask descriptor + true distance joint, or formally remove `generic` from the authoring enum. · Files: `packages/physics-rapier/src/joints.ts:117-127`; `packages/physics/src/backend.ts:620-633`; `packages/physics/src/components.ts`; `packages/physics/src/ecs-sync.ts` · **already tracked: PHYSICS_IMPLEMENTATION_PLAN.md Remaining Work item 3 (line 118)**

- **AI-8** · **Implement Rapier joint impulse readback + break-force + motor max-force + paired non-fixed frameB** · **P2** · **XL** · Four capability flags (`motorForceLimits`/`automaticBreakForce`/`jointImpulseReadback`/`pairedNonFixedFrameB`) are all `false`; `applyJointParameters` sets only limits+motors, frameB only feeds fixed joints, no impulse readback exists. Wire impulse readback, enforce breakForce (emit truthful `jointBreak` from physics), configure motor max-impulse, encode paired non-fixed frameB; flip flags true. · Files: `packages/physics-rapier/src/joints.ts:139-212`, `.../backend.ts`; `packages/physics/src/backend.ts:57-60,635-692`; `packages/app/src/systems/physics.ts:343-355` · **already tracked: PHYSICS_IMPLEMENTATION_PLAN.md Remaining Work item 4 (lines 119-120)**

- **AI-9** · **Add a persistent Rapier-handle→entity index for collision event resolution** · **P2** · **M** · Collision/contact/stay event resolution does nested O(bodies×colliders) `colliderMatchForHandle` scans, twice per pair, every step (Stay events re-scan all `activePairs`). Maintain a `Map<handle, match>` updated at upsert/destroy; replace scans with O(1) lookups. Optionally gate `CONTACT_FORCE_EVENTS` (currently always-on at threshold 0) behind opt-in. · Files: `packages/physics-rapier/src/colliders.ts:320-333`; `packages/physics-rapier/src/events.ts:101-128,196-244` · **✅ DONE — gap-fixes-batch-1 (`da7c2833`); re-verified 2026-06-08** — `buildColliderHandleIndex` built once/step in `collectRapierEvents`, O(1) lookups (`events.ts:32-36,104`). Legacy `colliderMatchForHandle` remains but is no longer the per-pair event path.

- **AI-10** · **Cook query-shape volumes from geometry for overlap/shape-cast** · **P3** · **M** · `queryShape` builds only Cuboid/Ball/Capsule/Cylinder/Cone and **throws** on convexHull/trimesh/heightfield — even though actual colliders now cook those shapes. Build `ConvexPolyhedron`/`TriMesh`/`Heightfield` query volumes paralleling the collider cooking. · Files: `packages/physics-rapier/src/shapes.ts:9-28` · **NEW (PHY-05 closed the collider half, not the query-volume half)**

### Rendering & GPU perf

- **AI-11** · **Gate the per-frame GPU drain on a pending GPU→CPU readback** · **P1** · **M** · ~~`waitForSubmittedWork` is awaited unconditionally every frame in five frame paths~~ **DONE (batch 9, 2026-06-09):** all five drains now gated on `frameBoundariesNeedGpuDrain` (readback boundary, GPU-timing readbacks, or occlusion readbacks/queries pending); `picking-frame.ts:342` left as-is. Residual follow-on: consume occlusion readbacks N frames later instead of same-frame. · Files: `packages/webgpu/src/app/report.ts`, `frame-loop.ts`, `sprite-frame.ts`, `custom-wgsl-frame.ts`, `mixed-custom-wgsl-frame.ts`, `queued-built-in-frame.ts` · **DONE**

- **AI-12** · **Route the forward + legacy scene paths through the frame graph and read userPassRegistry there (audit B4)** · **P1** · **L** · The public `addRenderPass`/`addComputePass` API executes **only** on the single-encoder post-effect graph path — it silently no-ops on the default forward render (`useFrameGraph` defaults false). Wire the forward-only and legacy multi-submit paths to read `userPassRegistry`. · Files: `packages/webgpu/src/app/app.ts:501-521`, `frame-loop.ts`, `user-pass.ts` · **already tracked: inline "audit B4" follow-up**

- **AI-13** · **Own and thread a persistent RenderExtractionCache across frames** · **P2** · **M** · The render-extraction change-detection cache is plumbed (`extraction.ts:43,109`) but unreachable: the only `createRenderExtractionCache` site is its own definition, and all shipped callers pass `{ frame }` only. Instantiate one cache per app instance and thread it into every `extractRenderSnapshot` call; validate `entityVersion` invalidation with a perf test. · Files: `packages/runtime/src/index.ts:325,329`; `packages/app/src/advanced.ts`; `packages/render/src/rendering/extraction.ts` · **NEW (MEDIUM_LONG_TERM_GOALS Tier 2 goal only)**

- **AI-14** · **Implement M9 positional indirect lighting (probes / irradiance volumes / hemisphere)** · **P2** · **XL** · Indirect light is a single global env; no reflection/light probes, irradiance volumes, GI, lightmaps, or hemisphere light. Add hemisphere/SH ambient, parallax-corrected reflection probes (generalize the single PMREM env to N probes), SH irradiance volumes (DDGI-lite), optional lightmap+UV2 import. · Files: `packages/webgpu/src/lighting`; `packages/render/src`; new probe ECS components · **already tracked: SOTA_ROADMAP M9 (0/4 not-started)**

- **AI-15** · **Add stateful particle simulation (velocity/age/forces) + view-space billboarding** · **P2** · **XL** · GPU particle compute is a stateless per-frame hash re-scatter on a world-XY disc (z forced to 0.0); no velocity/lifetime/forces/spawn-death, and the render quad is world-XY, not camera-facing. Read prior state (persistent/double-buffered velocity+age), integrate forces, spawn/kill by lifetime, add emitter shapes; build the quad in view space facing the camera. (Over-lifetime size/color curves already shipped via FEAT-01.) · Files: `packages/webgpu/src/render/particles/particle-pipeline.ts`, `packages/webgpu/src/app/particles.ts`, `packages/render/src/rendering/extraction-particles.ts` · **already tracked: FEAT-01 (curves only — does not cover motion/billboard)**

- **AI-16** · **Make particles depth-tested against scene geometry** · **P2** · **XL** · Particle render pipeline sets `depthWriteEnabled:false, depthCompare:'always'` so particles never depth-test against opaque geometry. Render with read-only depth (`depthCompare:'less-equal'`). (Pairs with AI-15's stateful storage; can be one ticket.) · Files: `packages/webgpu/src/render/particles/particle-pipeline.ts` (`createParticleRenderPipelineDescriptor` depthStencil ~lines 437-441) · **already tracked: FEAT-01 (out of scope)**

- **AI-17** · **Move all material families onto a shared output-stage tonemap/encode contract** · **P2** · **L** · `applyOutputTonemapToStandardShader` is applied only to the StandardMaterial family; unlit/matcap/debug-normal/sprite/MSDF-text/UI-quad/custom-WGSL/particle pipelines render untonemapped LDR, so mixed scenes without the HDR post path have inconsistent color. Factor the output color-space+tonemap wrapper into a shared contract and apply it across families (or enforce the HDR-post requirement). · Files: `packages/webgpu/src/output/output-stage-tonemap.ts:145`, `standard-pipeline.ts:117`, unlit/matcap/sprite/text/ui pipeline modules · **NEW**

- **AI-18** · **Make deferred clustered shadow/cookie sampling loud or guarantee variant match** · **P2** · **M** · On unsupported pipeline variants, clustered local-light shadow/cookie sampling silently returns a ~1.0 (unshadowed / white) sentinel when `SHADOW`/`COOKIE_SAMPLING_DEFERRED` is set — no render diagnostic. Guarantee the selected variant supports clustered shadows/cookies when metadata requests them, or surface a diagnostic. · Files: `packages/webgpu/src/materials/standard/standard-shader-light-sampling.ts`; `packages/webgpu/src/lighting/local-light-cluster-constants.ts` · **NEW**

- **AI-19** · **Add a normal target + thread the live view uniform into post-effect prepare()** · **P2** · **XL** · SSAO/SSR lack a normal G-buffer and the live camera projection: they bake near/far/fovY from static effect-construction constants (defaults 0.1/1000/π/3) and reconstruct normals from depth. Emit a packed normal target (or land the deferred G-buffer), add a `normal` field to `WebGpuPostEffectPrepareOptions`, and pass live camera params via the view uniform. · Files: `packages/webgpu/src/post/post-pass.ts:61-76`, `post-ssao.ts:504-514`, `post-ssr.ts:48-58`; forward pass / view-uniform plumbing · **already tracked: SOTA_ROADMAP M8-T9 (gated on M3)**

- **AI-20** · **Rewrite SSR as a general screen-space reflection** · **P2** · **L** · SSR is demo-grade: a hardcoded floor-only `receiverMask = smoothstep(0.48,0.78,uv.y)`, static camera params, depth-reconstructed normals, and a vertical-mirror miss fallback. Feed the live view/projection uniform, supply real normals (depends on AI-19's G-buffer), remove the floor mask and the mirror fallback. · Files: `packages/webgpu/src/post/post-ssr.ts:48-58,525,532,573-578` · **NEW**

- **AI-21** · **Add a spatial broad-phase (or worker/SoA culling) for render frustum culling** · **P3** · **XL** · Frustum culling is a single-threaded linear per-entity scan (O(views×planes) each); occlusion is opt-in query-feedback, no HiZ. Add a BVH/octree/grid broad-phase over world AABBs, or move culling onto SoA/worker fan-out. · Files: `packages/render/src/rendering/extraction-meshes.ts:60,112`, `extraction-culling.ts` · **NEW**

- **AI-22** · **Make indirect drawing GPU-driven and toggleable through the app path** · **P2** · **XL** · Indirect drawing is a 1:1 CPU-computed command substitution (no draw-call reduction, no GPU-driven culling, no `multiDrawIndexedIndirect`), and the app path always runs defaults — `enabled`/`minInstanceCount` are never plumbed through. Add a compute culling pass that writes the indirect arg buffer and/or multi-draw; thread `enabled`/`minInstanceCount`. · Files: `packages/webgpu/src/render/draw/indirect-draw-commands.ts:231-359`, `packages/webgpu/src/app/frame-boundary-support.ts:44-62` · **NEW**

- **AI-23** · **Realize transient aliasing with a shared descriptor+slot-keyed texture pool in the executor** · **P2** · **L** · Frame-graph transient resource aliasing is **analysis-only** (`audit L15`): `computeAliasing` produces assignments but the executor never consumes them — every transient allocates its own physical texture, no VRAM saved. Add a shared transient texture pool keyed by descriptor+slot; allocate from it in `frame-graph-execute.ts` using the existing assignments, with correct lifetime/barrier handling. _(Dedupes 3 identical gap entries into one ticket.)_ · Files: `packages/webgpu/src/render/graph/frame-graph-execute.ts`, `frame-graph-compile.ts:84-90` · **already tracked: inline `frame-graph-compile.ts:88` follow-up**

- **AI-24** · **Consume the simplified-mesh field via a MeshLodGroup + screen-error LOD selection** · **P2** · **L** · The simplified-mesh authoring field (formerly `simplifiedMeshId`, now minified) is written/defaulted but never consumed — distant instances render at full triangle count. Add a `MeshLodGroup` component (per-level mesh ids + thresholds), pack per-level index ranges into the snapshot, add screen-error/distance selection in extraction. · Files: `packages/render/src/rendering/authoring-components-spatial.ts`, extraction mesh selection, `packages/webgpu/src/render` (cull/draw) · **NEW (distinct from the by-design M8-T8 LOD-from-scratch item)**

- **AI-25** · **Make FrameGraph route config-driven and add it to the gating e2e set** · **P3** · **M** · The single-encoder FrameGraph path is shipped but gated behind a `?graph=1` URL flag (default OFF), and only 1 of the 4 gating CI specs exercises it — it can silently drift from the default path. Replace the URL flag with a config option; add a graph-route spec to the gating set; decide a default once at parity. · Files: `packages/app/src/browser/app.ts:83-94`, `.github/workflows/ci.yml:91-98` · **◐ PARTIAL — gap-fixes-batch-1 (`da7c2833`); re-verified 2026-06-08** — route now config-driven via `render.frameGraph` (`frame-graph-route.ts` `resolveUseFrameGraph`; legacy `?graph=1` still overrides). Residual: default still OFF (no parity flip) and CI still gates only `custom-graph-pass.spec.ts` — add a graph-route gating spec + flip the default at parity.

- **AI-26** · **Add proper LTC for disk/sphere area lights (and an area-light shadow path)** · **P3** · **XL** · Only rect area lights use real LTC; disk/sphere use crude analytic form-factor approximations and reuse the rect LTC specular term; no area-light shadows. Implement Heitz disk LTC + a sphere/tube model with per-shape specular. _(Confirm scope: the SOTA audit only mandates production rect LTC.)_ · Files: `packages/webgpu/src/materials/standard/standard-shader-source.ts:420-583`; `packages/webgpu/src/shadows/` · **NEW**

- **AI-27** · **Add a procedural sky / atmosphere model (sun-driven gradient or Bruneton-lite)** · **P3** · **L** · Only a static skybox cubemap exists — no scattering/sun-direction/time-of-day; not on any roadmap doc (competitors ship one, e.g. Bevy's Bruneton atmosphere). Add a sun-direction analytic sky (Preetham/Hosek or Rayleigh+Mie approx) renderable as background and feeding IBL ambient; wire a `SkyAtmosphere` component through extraction into a WGSL sky pass. · Files: `packages/render/src/rendering/extraction-skyboxes.ts`; `packages/webgpu/src/render` (new sky pass); `packages/render/src/rendering/authoring.ts` · **NEW**

- **AI-28** · **Query device-supported MSAA sample counts and widen the MSAA type** · **P3** · **M** · MSAA is hard-capped to exactly 1×/4×: type is `1|4`, the supported-count constant is `[1,4]`, and any `requested>1` collapses to 4 with no adapter capability query. Widen to include 2|8, query texture-format sample-count capability, choose nearest supported. _(Dedupes 2 identical gap entries.)_ · Files: `packages/webgpu/src/gpu/msaa.ts` (`resolveWebGpuMsaaConfig`, `WebGpuMsaaSampleCount`, `WEBGPU_SUPPORTED_MSAA_SAMPLE_COUNTS`) · **✅ RESOLVED — gap-fixes-batch-1 (batch 8, 2026-06-08); premise corrected** — WebGPU core only guarantees sampleCount 1 and 4 (no runtime capability-query API; three.js/PlayCanvas also use only 1/4 on WebGPU, and texture-creation validation is the real device check, already handled). Selection is now data-driven from `WEBGPU_SUPPORTED_MSAA_SAMPLE_COUNTS` via `nearestSupportedSampleCount` (round up to next supported, cap at max), so widening the set later is a one-line edit; `clamped` already signals a non-honored request. "Widen to 2|8" is not feasible against current WebGPU.

- **AI-29** · **Add a wide-gamut/HDR output path** · **P3** · **XL** · Output is locked to linear/sRGB 8-bit SDR (`OUTPUT_COLOR_SPACES = ['linear','srgb']`); no Display-P3/Rec.2020 or HDR display output. Add P3/Rec.2020 conversion, an `rgba16float` extended-gamut canvas config, PQ/HLG encode, and HDR-target tonemap operators. · Files: `packages/webgpu/src/output/output-stage-color-space.ts`, `output-stage-tonemap.ts`, canvas config · **NEW**

- **AI-30** · **Reuse persistent extraction scratch buffers instead of fresh `number[]` arrays per frame** · **P3** · **M** · Per-frame extraction allocates fresh JS `number[]` accumulators (transforms, bones, morphs, instanceTints, quadInstance\*, viewMatrices, instanceAttributes) — the residual GC churn the transfer-list + deleted-pool decision didn't address. Hoist them into reusable scratch buffers cleared per frame. · Files: `packages/render/src/rendering/extraction.ts:55-67` · **⛔ BLOCKED on AI-76 (benchmark harness) — do not implement blind** — FEAT-03 already showed naive buffer reuse ~3.9× _slower_, so this must be measure-then-hoist: land an extraction `*.bench.ts` (AI-76) first, then hoist the `number[]` accumulators (the returned typed arrays are copied per-frame, so the scratch can be reused safely once the perf win is proven, and the RenderExtractionCache writeback aliasing is checked). Cleanly actionable as a sequenced item: **AI-76 → AI-30**.

- **AI-87** · **Drive every authored env map through the PMREM prefilter; remove the stale "deferred"/placeholder fallback** · **P2** · **M** · IBL specular PMREM/GGX is real and engaged when a `pmremSource` is present (equirect→cube auto-derives it), but env maps that arrive unprepared / with no pmrem source still hit the placeholder upload and emit `specularProofUploadPlaceholder`/`specularPrefilteringDeferred`, and `ibl-resource-descriptor.ts` emits an `iblResourceDescriptor.{diffuse,specular}Unsupported` placeholder. Populate `pmremSources` for all authored env maps (cubemap-direct as well as equirect-derived) so the placeholder/deferred path is unreachable; drop the stale diagnostics. · Files: `packages/webgpu/src/lighting/ibl-resource-descriptor.ts:179-193`, `ibl-texture-resource-specular.ts:545-567`, `diffuse-ibl-resource-summary.ts:197-215` · **◐ PARTIAL — promoted from §6 into the numbered list on 2026-06-08; re-verified 2026-06-08**

### Assets & Scene

- **AI-31** · **Add a reusable fly / first-person (WASD + pointer-look) camera controller** · **P3** · **M** · Only an orbit controller is a reusable barrel-exported API (`createOrbitCameraController`); there is no fly/FPS/free-look/WASD controller anywhere. Add `createFlyCameraController` (and an optional focus/look-at helper) consuming the existing action/pointer system; export from the controllers barrel. · Files: `packages/app/src/controllers/` (new `fly-camera.ts`), `controllers/index.ts` · **✅ DONE — gap-fixes-batch-1 (`7d5e035c`); re-verified 2026-06-08** — `createFlyCameraController` shipped and barrel-exported from `@aperture-engine/app`. (An e2e example is a nice-to-have follow-on.)

- **AI-32** · **Add Light/Camera to the glTF ECS command plan + KHR_lights_punctual / node.camera parsing** · **P2** · **L** · The engine glTF importer's command plan has only 8 component names (no Light/Camera); `KHR_lights_punctual` is absent from `SUPPORTED_ROOT_EXTENSIONS` and `node.camera` is never traversed — light/camera import lives only in the example worker. Extend the plan + replay switch with Light/Camera, add the extension, traverse `node.camera`. _(Note: partly deferred-by-design for M1 per SOTA, but a real engine-importer gap.)_ · Files: `packages/render/src/assets/gltf-ecs-authoring-command-plan-types.ts:6-14`, `gltf-ecs-command-replay-components.ts`, `gltf-root.ts:38-46`, `gltf-scene-traversal-nodes.ts` · **NEW**

- **AI-33** · **Parse KHR_lights_punctual into Light authoring commands during scene import** · **P2** · **M** · Scene import requires a caller-supplied `GltfSceneDirectLightIntent` and diagnoses `gltfSceneImport.missingDirectLightIntent`; lights embedded in a `.glb` are never turned into engine lights automatically. Read the extension and node light refs during import; make the intent a fallback. _(Overlaps AI-32; can share the parsing work.)_ · Files: glTF scene import path, `gltf-scene-import-contract-types.ts:66`, `gltf-scene-import-contract-diagnostics.ts:98-101` · **NEW**

- **AI-34** · **Apply sparse-accessor index/value overrides in the reader** · **P2** · **M** · Sparse accessors are silently zero-filled: the float reader returns a zero array when the bufferView is absent, and the validator only emits `gltfAccessor.sparseAccessorDeferred`. Read `accessor.sparse.indices`/`.values` and overwrite the corresponding entries; upgrade the validator. · Files: `packages/render/src/assets/gltf-accessor-float-reader.ts:158-163`, `gltf-accessor-validation-accessors.ts:39-47` · **◐ PARTIAL — gap-fixes-batch-1 (`fecaf08c`); re-verified 2026-06-08** — `applyGltfSparseOverride` now applies sparse in the float (skin/animation) reader. Residual: the **primitive vertex-attribute** path still emits `gltfAccessor.sparseAccessorDeferred` (`gltf-accessor-validation-accessors.ts:41`).

- **AI-35** · **Define an on-disk / self-contained scene format with a file/URL loader + asset rebinding** · **P3** · **XL** · `saveScene`/`loadScene` are in-memory ECS-state-only JSON (no mesh/material/texture bytes, no file I/O, no streaming); a saved doc isn't self-contained and asset handles only resolve if the original glTF is re-imported into the same registry; `loadScene` also has an elics last-registering-world component-storage rebind footgun. Add a versioned container (asset manifest + content hashes, optional embedded bytes), a file/URL writer+async loader, asset rebinding on load, and decouple from the storage-binding order. _(Dedupes 4 overlapping gap entries into one program.)_ · Files: `packages/simulation/src/serialization/scene-document.ts`, `component-codec.ts`, `component-registry.ts`, `prefab.ts`; new scene-loader + CLI file-I/O command · **NEW**

- **AI-36** · **Add an offline `aperture build/bake` asset command wrapping encoders** · **P3** · **XL** · No asset-processing/baking pipeline exists in CLI or build (all decode is runtime browser-only). Add a CLI command wrapping KTX2/basisu transcoding, Draco/meshopt compression, and mesh decimation (e.g. gltf-transform/toktx) to produce optimized glTF/GLB offline. · Files: `packages/cli/src/cli.ts:55-105`, new `packages/cli/src/commands/bake.ts` · **NEW (DC-02 removed dead scaffold only)**

- **AI-37** · **Add a glTF/GLB writer and scene-document file exporter** · **P3** · **XL** · The engine is import-only; no glTF/GLB writer or scene exporter of any kind. Implement a glTF/GLB writer + an ECS-scene-to-file exporter (optionally a CLI export command) for round-tripping authored content. · Files: new `packages/render/src/assets/gltf-exporter*`, `packages/cli/src/commands/`, simulation scene serialization · **NEW**

- **AI-38** · **Add per-extension parse + shader support for missing PBR-next extensions** · **P3** · **XL** · `SUPPORTED_MATERIAL_EXTENSIONS` covers only unlit/clearcoat/transmission/sheen/iridescence/ior/volume; `clearcoatNormalTexture` is preserved-but-not-rendered; emissive_strength / specular / anisotropy / variants / animation_pointer are dropped. Implement parse + shader support for the missing ones and sample `clearcoatNormalTexture`. · Files: `packages/render/src/materials/gltf-material-extensions.ts:10-41`, PBR shader sources · **NEW**

- **AI-39** · **Support non-triangle glTF primitive modes** · **P3** · **L** · Only TRIANGLES is imported; POINTS/LINES/LINE_LOOP/LINE_STRIP/TRIANGLE_STRIP/TRIANGLE_FAN emit `gltfMesh.unsupportedPrimitiveMode` and are dropped. Add strip/fan→triangle-list conversion plus point-list/line-list/line-strip pipelines + topology mapping. · Files: `packages/render/src/assets/gltf-mesh-primitive-planning.ts`, render pipeline topology handling · **NEW**

- **AI-40** · **Wire JOINTS_1/WEIGHTS_1 and extra attribute slots** · **P3** · **XL** · The attribute mapper stops at WEIGHTS_0, capping skinning at 4 influences/vertex; TEXCOORD_2+ and COLOR_1 are lost (they exist only as unused union members). Read JOINTS_1/WEIGHTS_1 (8-influence palette) and add the extra UV/color slots. · Files: `packages/render/src/assets/gltf-mesh-primitive-attributes.ts`, `packages/render/src/mesh/types.ts`, skinning shader/layout · **NEW**

- **AI-41** · **Import morph TANGENT deltas and add a full-N morph render path** · **P3** · **L** · Only POSITION+NORMAL morph deltas are imported (no TANGENT); beyond-first targets aren't rendered. Decode TANGENT deltas and add the documented morph-target data-texture render path. · Files: `packages/render/src/assets/gltf-morph-target-import.ts`, morph render path · **◐ PARTIAL (re-verified 2026-06-08)** — the all-N morph-target import lift (target-major `importGltfMorphTargets`) already landed (pre-2026-06-07). Residual: decode TANGENT deltas (POSITION+NORMAL only today).

- **AI-42** · **Parse EXT_mesh_gpu_instancing into per-instance transforms** · **P3** · **L** · `EXT_mesh_gpu_instancing` is not in `SUPPORTED_ROOT_EXTENSIONS`; instanced glTF scenes collapse to a single node. Add the extension and read `node.extensions.EXT_mesh_gpu_instancing` during traversal to author ECS instances. · Files: `packages/render/src/assets/gltf-root.ts`, scene/node traversal + authoring plan · **NEW**

- **AI-43** · **Add CORS-aware cross-origin GLB buffer/image URI resolution** · **P3** · **M** · GLB external buffer/image URI fetching is same-origin only — `resolveSameOriginUrl` rejects any differing origin, so CDN/cross-origin `.bin`/textures cannot load. Replace the rejection with CORS-aware resolution (appropriate fetch credentials/mode). · Files: `packages/render/src/assets/glb-uri-external-fetch-resolve.ts:59-100`; `docs/GLB_FIXTURE_LIMITATIONS.md` · **NEW (doc Deferred list only)**

### Platform — Audio / UI / Text / XR / Input / A11y / Net / i18n

- **AI-44** · **Forward `pointerId`/`pointerType`/`button` and model multiple concurrent pointers** · **P2** · **L** · The browser forwarder hard-codes `pointer:'primary'` for every event and never reads `event.button`/`pointerType`; the input state machine returns early for non-primary and models only `pointer.primary` — so secondary/middle bindings, multi-touch, and gestures can never fire. Map `event.button`→primary/secondary/middle, track multiple `pointerId`s, extend to a multi-pointer state model with gesture recognizers. _(Closely related to AI-46, AI-58.)_ · Files: `packages/app/src/browser/input.ts:52-81`, `packages/app/src/input/state.ts:125`, `packages/app/src/interaction/pointer-events.ts` · **NEW**

- **AI-45** · **Add a composition-aware DOM input bridge and editable text model** · **P2** · **XL** · No editable text / IME / caret / selection: the forwarder installs no `input`/`beforeinput`/`composition*` listeners and `UiText` is render-only. Install those listeners via a hidden contenteditable/textarea overlay, surface text/composition state through the input resource, add a caret/selection model and an editable UI text component. · Files: `packages/app/src/browser/input.ts:49-127`, `input/keyboard.ts`, `input/state.ts`, `types.ts`, render text/UI components · **NEW**

- **AI-46** · **Add a UI focus subsystem with keyboard/gamepad navigation** · **P2** · **L** · No UI focus model or keyboard nav: interaction reads only `pointer.primary`, holds a single hover/down with no focus concept — no focus ring, tab order, Enter/Space activation, or spatial nav. Add focusable flags, tab/spatial order, focus visuals, and Enter/Space (+ D-pad) activation wired through the action system. · Files: `packages/app/src/interaction/system.ts:26`, `pointer-events.ts:65-67`, `access.ts`, UI component defs in `packages/render/src` · **NEW**

- **AI-47** · **Add a wheel input event kind and a UiScroll wheel/drag mapping system** · **P1** · **L** · No mouse-wheel transport: there is no `wheel` listener, and `readScrollOffset` returns the statically authored `UiScroll.offset` — UI scroll never moves. Add a `wheel` listener emitting a wheel event kind (deltaX/Y), expose it on the input resource, and add a UI system mapping wheel/drag→`UiScroll.offset` with clamping/inertia (optionally route to orbit zoom). · Files: `packages/app/src/browser/input.ts`, `packages/app/src/input` (transport/resource), `packages/render/src/rendering/extraction-ui.ts` · **NEW**

- **AI-48** · **Make secondary/middle pointer bindings reachable or reject them in validation** · **P1** · **M** · Virtual-action bindings with `pointer:'secondary'|'middle'` validate and type-check but are **dead at runtime** with no diagnostic (bindings resolve only `primary`; forwarder emits only `primary`). Either forward per-button events end-to-end (depends on AI-44) or add a validation diagnostic rejecting non-primary pointer bindings until supported. · Files: `packages/app/src/browser/input.ts`, `input/bindings.ts`, `config/validation.ts:275-281` · **✅ DONE (validation slice) — gap-fixes-batch-1 (`76e77727`); re-verified 2026-06-08** — `validatePointerName` now throws `aperture.config.invalidInputBinding` for secondary/middle bindings (`validation.ts:274-287`). The full per-button forwarding alternative remains **AI-44**.

- **AI-49** · **Add gamepad haptics, pointer-lock, and a remapping surface** · **P2** · **L** · Gamepad now correctly disconnects+warns on non-standard mapping (fixed), but still has **no remapping/calibration** (`GAMEPAD_BUTTON_INDICES` hardcoded), **no haptics/vibration**, and **no pointer-lock**. Add a `vibrationActuator` haptics API, `requestPointerLock`/`pointerlockchange` handling for FPS mouse-look, and a custom mapping adapter so non-standard controllers can be remapped instead of dropped. · Files: `packages/app/src/input/gamepads.ts`, `input/bindings.ts`, `browser/input.ts` · **NEW**

- **AI-50** · **Add chord/modifier/timing binding kinds to the input action system** · **P3** · **L** · No chords/combos/hold/double-tap/modifier gating; `#resolveActions` resolves each binding independently and binding kinds have no chord/modifier kind. Add declarative chord (multi-key AND), required-modifier gating, hold/long-press, and double-tap/sequence kinds with cross-frame timing state. · Files: `packages/app/src/input/bindings.ts`, `input/state.ts`, binding config types · **NEW**

- **AI-51** · **Model multiple pointers in input state and pick per-pointer in the interaction driver** · **P3** · **L** · Default interaction picking is single-pointer (input drops non-primary), bounds-source, one ray/frame. Stop dropping non-primary pointer events (indexed pointer map), iterate all active pointers in `runInteractionFrame`, optionally add a collider-source pick default. _(Overlaps AI-44; sequence after it.)_ · Files: `packages/app/src/input/state.ts:124-127`, `interaction/system.ts:26-84` · **NEW**

- **AI-52** · **Integrate a text shaping + bidi + font-fallback pipeline for MSDF text** · **P3** · **XL** · Text layout is LTR-only: complex scripts are detected and emit `msdfText.unsupportedShaping`, then dropped — no shaping engine, bidi reordering, grapheme clustering, or font-fallback chain. Integrate a HarfBuzz-style wasm shaper, Unicode bidi, grapheme segmentation, and a multi-atlas fallback chain. · Files: `packages/render/src/text/msdf-font-atlas.ts` (`diagnoseUnsupportedShaping` ~1016, layout ~812-882, `missingGlyphAdvance` ~1060) · **NEW**

- **AI-53** · **Adopt a Taffy-compatible flex/grid layout solver behind the existing layout adapter** · **P2** · **XL** · UI layout supports only Absolute/Row/Column — no flex grow/shrink/basis, wrap, grid, justify-content, or align-items (cursor-based flow). Replace the hand-rolled flow with a Taffy-compatible (or minimal-port) solver per M6 Open Decision #1. · Files: `packages/render/src/rendering/authoring-types.ts:102-106`, `extraction-ui.ts`, `docs/M6_UI_PARTICLES_NOTES.md` · **NEW**

- **AI-54** · **Add clipboard integration for in-engine text fields** · **P3** · **L** · No clipboard read/write or copy/cut/paste handling. Wire `navigator.clipboard.read/writeText` behind a user-gesture-guarded action and copy/cut/paste events tied to a selection model. _(Depends on AI-45's editable-text/selection model.)_ · Files: `packages/app/src/input/` (new clipboard module) · **NEW**

### Architecture & Scheduler

- **AI-55** · **Add a real sim clock (fixed-rate accumulator) + renderer ack/backpressure + frame-stamped snapshots** · **P2** · **XL** · The generated sim loop is a free-running `setTimeout(0)` driven by wall-clock delta with no fixed-rate decoupling, no renderer→worker ack/backpressure, and no per-frame sim id. Replace with a fixed-rate clock+accumulator, add an ack/backpressure message to `SIMULATION_WORKER_PROTOCOL`, and stamp each snapshot with its sim frame for replay/lockstep. _(Dedupes the loop-pacing + wall-clock + frame-pacing entries.)_ · Files: `packages/app/src/worker/loop.ts:127-141`, `packages/runtime/src/simulation-worker.ts:4-10`, `shared-snapshot-transport.ts` · **NEW**

- **AI-56** · **Implement a SAB-backed frame-stamped input command ring buffer with deterministic per-frame drain** · **P2** · **XL** · Input crosses the worker boundary as unordered per-event `postMessage`s accumulated into `pendingInput` and `splice`d each frame — no SAB ring, no per-sample sim-frame timestamps, no coalescing. Add a frame-stamped SAB ring and a deterministic per-frame drain in `advanceGeneratedInputFrame` (enables replay/lockstep). · Files: `packages/app/src/worker/loop.ts:34`, `worker/snapshot.ts:74-78`, new input ring transport in `packages/runtime/src` · **NEW**

- **AI-57** · **Introduce a richer ECS scheduler (named sets + before/after + run conditions)** · **P2** · **XL** · The scheduler is a flat integer priority with an alphabetical tie-break; schedule metadata carries only `{ priority }` — no system sets, before/after constraints, run conditions, or engine-phase anchors. Layer a declarative scheduler over elics priority ordering. · Files: `packages/app/src/systems.ts`, `advanced.ts` · **NEW (NORTH_STAR goals only)**

- **AI-58** · **Expose built-in frame phases as named schedule anchors** · **P2** · **L** · Engine-internal phases (animation, fixed-step, transform resolution, skinning) are hardcoded in `runtime` `step()` and `app` `advanced.ts` bracketing; only `'input'`/`'postUpdate'` phase strings are exposed. Add named anchors (`afterPhysicsWriteback`, `afterTransformResolution`, `beforeSkinning`) that user systems can target via before/after. · Files: `packages/runtime/src/index.ts:295-308`, `packages/app/src/advanced.ts:177-188` · **NEW (DOC-07 is render-taxonomy only)**

- **AI-59** · **Introduce a packaged-feature Plugin abstraction with a lifecycle** · **P2** · **XL** · No unified plugin/extension model: `ApertureSystemModule` is systems-only and registration has no bundled component/resource/render-pass/config and no build/finish/cleanup lifecycle (render-graph is separately extensible only). Add `createPlugin`/`definePlugin` registering all of those as one unit + an `addPlugin` entry; migrate ad-hoc `registerComponent`/loose modules onto it. · Files: `packages/app/src/advanced.ts:33-43,238`, `packages/app/src/systems/components.ts`, new plugin module · **NEW (direction noted in DEVELOPER_API_PROPOSAL.md)**

- **AI-60** · **Add dirty-flag transform propagation, incremental spatial index, and remove the double resolve/refresh per step()** · **P2** · **XL** · World-transform resolution and spatial-index population fully rebuild every frame (no dirty propagation) **and run twice per `step()`** (`resolveWorldTransforms` at `advanced.ts:178` then again at `runtime/index.ts:304`; `refreshSpatialIndex` at `advanced.ts:179` and `:183`). Add per-subtree dirty flags, make spatial population incremental, and collapse the redundant double resolve/refresh. · Files: `packages/simulation/src/transform/resolution.ts`, `packages/app/src/systems/spatial-index-population.ts`, `advanced.ts`, `runtime/index.ts` · **NEW**

- **AI-61** · **Pace the generated worker loop at a target Hz** · **P2** · **M** · The generated worker paces snapshot production on `setTimeout(tick,0)`, not rAF or a clock-paced loop; fixed-step plumbing feeds `app.step` only, not loop cadence. Replace with an accumulator-driven loop at a configurable target Hz (or coordinate publish cadence with render rAF). _(Subsumed by AI-55 if done together; tracked separately for the narrower fix.)_ · Files: `packages/app/src/worker/loop.ts` · **NEW**

- **AI-62** · **Move ECS change-tracking off per-instance method patching to intercept indexed vector writes** · **P2** · **L** · Change detection monkey-patches `addComponent/removeComponent/setValue/getVectorView/destroy` per entity instance and wraps only `view.set` — so `view[i]=x` indexed writes bypass `bumpEntityVersion`. Replace with Proxy-backed vector views (or change tracking inside elics storage) so indexed writes bump the version; add a regression test. · Files: `packages/simulation/src/ecs/index.ts:69-161` · **NEW**

- **AI-63** · **Delete the entity version key in `patchedDestroy` to bound the change-detection map** · **P2** · **S** · `versionByEntityKey` only ever `.get()/.set()` — never `.delete()`; since elics recycles slots with an incremented generation, every incarnation makes a fresh key and the map grows unbounded under spawn/despawn churn. In `patchedDestroy`, `delete` the version key (recycled slot restarts at 0; safe because consumers key by `index:generation`). Add a churn-loop size-bound test. · Files: `packages/simulation/src/ecs/index.ts:133-141,163-170` · **✅ DONE — gap-fixes-batch-1 (`76e77727`); re-verified 2026-06-08** — `patchedDestroy` now `versionByEntityKey.delete(key)` (`ecs/index.ts:151`); `entityVersionTrackingSize()` accessor added; churn-bound test.

- **AI-64** · **Wire `RenderSnapshotUpdateSchedule` / per-entity version into dirty-range buffer uploads** · **P2** · **XL** · The snapshot change-set/update-schedule gates render-bundle reuse but never drives incremental GPU uploads, and `writeBufferData` always writes the whole buffer from offset 0. Thread the per-family schedule + per-entity version (already shipped in `ecs/index.ts`, consumed by `extraction-mesh-cache`) into the upload path: skip writes when nothing changed (mirror the existing local-light-cluster skip) and emit contiguous dirty ranges with a full-write fallback above a dirty-fraction threshold. _(Dedupes the transform-specific, generic-buffer, and schedule-driven dirty-range entries into one program; see AI-65/AI-66 for the follow-on generalization.)_ · Files: `packages/render/src/rendering/transform-pack.ts:77`, `packages/webgpu/src/app/app-frame-resource-utils.ts:18-31`, `packages/webgpu/src/materials/standard/standard-app-frame-resources.ts:401-421`, `snapshot-update-scheduler.ts` · **NEW (OPTIMIZATIONS_TRANSPORT_AND_UPLOAD.md item 1, "not currently in the roadmap")**

- **AI-65** · **Generalize dirty-range sub-range upload to view/light/material buffers** · **P3** · **L** · After AI-64's transform dirty-range lands, extend it to the smaller per-frame buffers (view matrices change on camera change, light data on light change, material uniforms rarely) with per-buffer change versions. · Files: `packages/webgpu/src/app/app-frame-resource-utils.ts:18-31`, `standard-app-frame-resources.ts:290-421` · **NEW (OPTIMIZATIONS_TRANSPORT_AND_UPLOAD.md item 3)**

- **AI-66** · **Add a dirty-range-aware buffer upload primitive** · **P3** · **L** · The enabling primitive for AI-64/65: extend `writeBufferData` (or a sibling) to accept a `bufferOffset`/length sub-range, and have dynamic-buffer producers emit min/max dirty offsets. · Files: `packages/webgpu/src/app/app-frame-resource-utils.ts:18-31`, dynamic-buffer producers · **NEW**

- **AI-67** · **Separate transform-only updates from full packet rebuilds in mesh extraction** · **P3** · **L** · Extraction reuse keys on `entityVersion`, but `writeWorldTransform` calls `markEntityChanged` on _any_ world-matrix column change, so any moving/animated entity bumps the version every frame and invalidates its cache — reuse only ever helps perfectly static entities. Add a transform-version distinct from structural `entityVersion` (or reuse the packet template and only re-write transform/bounds when only the matrix changed). · Files: `packages/render/src/rendering/extraction-meshes.ts`, `extraction-mesh-cache.ts`, `extraction-mesh-cache-writeback.ts`, `packages/simulation/src/transform/resolution.ts` · **NEW (FEAT-05 was a different backend cache)**

- **AI-68** · **Design a parallel system scheduler with conflict analysis and a worker pool** · **P3** · **XL** · No intra-frame parallelism — sim systems run strictly serially; no worker pool, job graph, or SAB-backed cross-thread component storage. Add read/write conflict analysis over the component model, a worker-pool job graph for non-conflicting systems, and multi-thread-usable component storage. **Gate behind a design doc first.** _(Dedupes the two parallelism/scheduler entries.)_ · Files: `packages/runtime/src/index.ts:296-307`, `packages/app/src/worker/loop.ts`, `packages/app/src/systems.ts`, `packages/simulation/src` (component storage) · **NEW (ADVANCED_ORCHESTRATION.md planned only)**

### Architecture — Transport (partial residue)

- **AI-69** · **Emit COOP/COEP dev-server headers from the vite-plugin and surface transport in the generated scaffold** · **P2** · **M** · The SAB transport now defaults to `'auto'` and auto-upgrades when cross-origin isolated (FEAT-02), but the vite-plugin dev server emits **no COOP/COEP headers**, so default-scaffolded apps aren't isolated and `'auto'` falls back to transferable; `StartGeneratedBrowserAppOptions` has no transport field. Add COOP/COEP middleware (config-gated) in `configureServer`, optionally thread a transport field through the scaffold, and document a production COOP/COEP recipe. · Files: `packages/vite-plugin/src/index.ts:82-93`, `virtual-modules.ts:67-82`, `packages/app/src/browser/app.ts:27-37,88-100` · **already tracked: FEAT-02 (closed the auto-select half, not headers)**

- **AI-70** · **Extend SAB transport to source-assets and the remaining packet families** · **P2** · **L** · The SAB write side is driven by the default worker, but `sourceAssets` is re-serialized and `postMessage`d **every frame on both paths**, and `hasUnsupportedSharedSnapshotPayload` forces the transfer-list fallback for sprites, particles, UI nodes/hit-regions, skyboxes, fogs, instance-attribute packets, bones, and morphs — so many real scenes still avoid SAB. Move source-assets off per-frame postMessage (send-on-change) and add shared-packet encoders for the unsupported families. · Files: `packages/app/src/worker/snapshot.ts`, `packages/runtime/src/shared-snapshot-transport.ts`, `packages/webgpu/src/app/app-snapshot-transport.ts` · **◐ PARTIAL — gap-fixes-batch-1 (batch 8, 2026-06-08)** — pt.1 done: `publishGeneratedWorkerSnapshot` now attaches `sourceAssets` only when an entry changed (steady-state frames omit it; the receiver no-ops on an absent/empty field). Residual (pt.2, cleanly actionable, L): shared-packet encoders for sprites/particles/UI/skyboxes/fogs/instance-attrs/bones/morphs in `encodeSnapshotPackets` + `shared-snapshot-transport.ts` `writeFrame`/reader, then drop those families from `hasUnsupportedSharedSnapshotPayload`.

### Tooling / CI / Docs

- **AI-71** · **Expand CI e2e coverage with sharding and a real-GPU OS matrix** · **P1** · **L** · CI runs exactly 4 of 145 e2e specs, on SwiftShader, ubuntu-latest + node 24 only — ~97% of e2e proofs are non-gating. Shard the full suite into CI, add a macOS runner for a real Metal GPU, budget per-test timeouts, keep SwiftShader as the Linux fallback. · Files: `.github/workflows/ci.yml`, `playwright.ci.config.ts` · **NEW**

- **AI-72** · **Cut the first real `v*` release and enable a changelog** · **P1** · **M** · All packages are unpublished (`npm view` → E404), no `v*` tag, `.changeset/config.json` changelog `false` (14 pending changesets staged). Flip changelog to a generator, consume the changesets, push a `v*` tag to trigger `release.yml`, and verify `workspace:*`→version conversion resolves so `npx @aperture-engine/cli create` works externally. · Files: `.changeset/config.json`, `.changeset/*.md`, `.github/workflows/release.yml`, `packages/*/package.json` · **NEW (SOTA M1-T11 marks release machinery done)**

- **AI-73** · **Route the user-pass API onto the forward path** — _see AI-12 (Rendering)_. Listed there.

- **AI-74** · **Adopt Playwright `toHaveScreenshot` golden baselines on a pinned deterministic renderer** · **P2** · **L** · No golden-image visual regression — e2e asserts only relative pixel deltas, so a wrong-but-different gamma/shading regression that preserves delta direction is undetectable. Add per-route screenshot baselines with a `maxDiffPixelRatio` tolerance against a pinned SwiftShader/headless image; start with canonical scenes (lit mesh, shadows, tonemap, post-effects). · Files: `test/e2e/*.spec.ts`, `playwright.config.ts`, new `test/e2e/__snapshots__`, `scripts/render-control.mjs` · **NEW**

- **AI-75** · **Distinguish "environment cannot test" from "feature broken" and gate skip counts** · **P2** · **M** · Many e2e pixel asserts silently become green `test.skip(true, …)` no-ops when GPU readback is unavailable (orthographic, ecs-triangle, physics-character). Make pixel-proof specs fail/error when an expected-capable environment lacks readback (capability flag at suite start) and report the runtime skip count as a CI gate. · Files: `test/e2e/orthographic-camera.spec.ts`, `ecs-triangle.spec.ts`, `physics-character.spec.ts`, shared readback/capability helper · **NEW**

- **AI-76** · **Commit render/extraction micro-benchmarks + a vitest bench config** · **P2** · **L** · Only physics is benchmarked; no `*.bench.ts`, no bench script/config, and the "benchmark" tests assert only determinism (finite/≥0), not perf budgets. Add `*.bench.ts` for extraction, transform packing, frustum culling, draw-list coalescing, buffer upload; a vitest bench config + script; and a scene-scale frame-time harness so the OPTIMIZATIONS\__ decision triggers (>1ms writeBuffer, >2ms encode) are measurable, with CI budget thresholds. _(Dedupes the two benchmark gaps.)_ · Files: `vitest.config.ts`, `package.json`, `test/physics/benchmark.test.ts` (extend or new), `packages/render/src` + `packages/webgpu/src` (new `_.bench.ts`) · **NEW (FEAT-03 ran a throwaway benchmark, committed no harness)**

- **AI-77** · **Enable vitest coverage with per-package thresholds in the CI gate** · **P2** · **M** · No code-coverage measurement or gate: no coverage config, no installed `@vitest/coverage-v8` (only an optional peer in the lockfile), no `--coverage` in test/check. Add the dev dep, a v8 coverage block (lcov+text, `include packages/*/src`), per-package/global thresholds, and run `vitest run --coverage` in the `check` aggregate/CI. · Files: `vitest.config.ts`, `package.json`, `.github/workflows/ci.yml` · **NEW**

- **AI-78** · **Add dependency audit + code scanning to CI** · **P2** · **M** · No CI security tooling — no `pnpm audit`, dependabot/renovate, or CodeQL/SAST (DOC-09 added knip dead-code only). Add `.github/dependabot.yml` (or renovate) and a CI job running `pnpm audit` + CodeQL (or trivy/snyk) against published packages + CLI deps. · Files: `.github/dependabot.yml` (new), `.github/workflows/ci.yml` or new security workflow · **NEW**

- **AI-79** · **Drop the docs-freshness date check from the CI-gating `check` script** · **P2** · **S** · `check:progress` is a time-bomb: it fails the build when `docs/index.html`/`render-pipeline-comparison.html` are >7 days stale (`MAX_AGE_DAYS=7`, `process.exit(1)`), giving long-lived branches and external contributors spurious red builds. Remove (or make non-fatal) the freshness assertions from the CI path; keep the structural phase-board checks; optionally move freshness to an opt-in scheduled job. · Files: `scripts/check-progress-tracker.mjs:3-6,20-27,65-69`, `package.json` (check script) · **✅ DONE — gap-fixes-batch-1 (`76e77727`); re-verified 2026-06-08** — staleness is now a non-fatal warning unless `APERTURE_PROGRESS_TRACKER_ENFORCE_FRESHNESS=1`; structural phase-board checks stay fatal (logic extracted to a pure `evaluateProgressTracker()`).

- **AI-80** · **Generate API reference and publish a public docs/example site** · **P2** · **XL** · No generated API reference and no public docs/example site — `docs/index.html` is a status dashboard, and DOC-06 only linked the _internal_ example gallery. Add typedoc/api-extractor over the typed surface, build a public docs site, and turn `examples/` into a hosted runnable gallery. · Files: `package.json` (root + per-package), `docs/index.html`, `examples/index.html`, new docs site config · **NEW (real fix is SOTA M11-T5, not-started)**

- **AI-81** · **Ship an R3F-style React binding over ECS commands** · **P2** · **XL** · No framework integrations (React/R3F, Vue, Svelte); the only consumption surface is the imperative app/worker path. Create `packages/react` (then vue/svelte) providing a declarative reconciler over ECS commands per SOTA M11-T2. · Files: new `packages/react/`, `pnpm-workspace.yaml`, `packages/app` exports · **NEW**

- **AI-82** · **Rename "embedding-backed" to honest "lexical reference search" (or integrate a real local embedding model)** · **P2** · **S** · The Reference RAG advertised as "embedding-backed" is a FNV-1a hashed bag-of-words (384-dim, alias table); no semantic model exists. Cheap close: reword `docs/AI_TOOLING.md:54` and RAG surfaces to "lexical/keyword reference search." Full close: swap `embedReferenceText` for a local ONNX/transformers.js model behind the existing `cosineSimilarity` interface. · Files: `docs/AI_TOOLING.md:54`, `packages/cli/src/reference/embedding.ts`, `reference/search.ts` · **◐ PARTIAL — gap-fixes-batch-1 (batch 8, 2026-06-08)** — the honesty reword landed: `docs/AI_TOOLING.md:54` now states the reference tools use lexical (hashed bag-of-words) keyword search, not learned/semantic embeddings (the on-disk `aperture-reference-hash-embedding` contract id was already honest). Residual (the real-embedding-model swap) deferred — it rewrites the breaking on-disk index contract.

- **AI-83** · **Build the M11 inspector on the existing MCP/devtools backend** · **P2** · **XL** · No editor or in-browser end-user devtools — devtools are DEV-only and gated on `aiDevtoolsEnabled`. Expose a human-facing inspector (entity tree, component editor, viewport selection, stats overlay) reusing the agent/MCP backend. · Files: `packages/app/src/browser`, new editor package, `packages/vite-plugin` (virtual-modules wiring) · **NEW (SOTA M11-T4 not-started)**

- **AI-84** · **Implement state-preserving system HMR via the vite-plugin (M11-T3)** · **P3** · **XL/L** · No state-preserving HMR: the aperture plugin has no `handleHotUpdate` and generated entries emit no `import.meta.hot.accept`, so editing a system/config triggers a full reload that wipes ECS world state. Add `handleHotUpdate` + `import.meta.hot.accept` in the generated worker/browser entries and a system reload path that re-registers against the live world (asset HMR as a follow-on). _(Dedupes 3 HMR entries.)_ · Files: `packages/vite-plugin/src/index.ts:71-112`, `virtual-modules.ts:57-85`, `dev-session.ts`, `system-discovery.ts`, `packages/app/src` (system registration/World lifecycle) · **NEW (SOTA M11-T3 not-started)**

- **AI-85** · **Adopt the official MCP SDK in the CLI MCP server** · **P3** · **M** · The MCP server is a hand-rolled JSON-RPC loop (hardcoded protocol version, manual newline buffer parser, manual `JSON.parse`, hand switch-dispatch); no `@modelcontextprotocol` dependency. Replace with the official SDK transport/dispatch. _(No action item was supplied in the data; included as the obvious remediation; lower priority.)_ · Files: `packages/cli/src/mcp.ts` · **NEW**

- **AI-86** · **Replace machine-specific doc paths with portable references** · **P3** · **M** · 13 docs reference hardcoded `/Users/felixz` absolute paths and `setup-references.sh` clones 6 pinned upstream engines; a new maintainer can't follow citations. Replace with repo-relative `references/` paths or upstream URLs+commit SHAs (the SHAs already pinned in `setup-references.sh`). · Files: `docs/MVP_3D_CONCEPTS.md`, `DEVELOPER_API_PROPOSAL.md`, `RAG_REFERENCE_PLAN.md`, `INPUT_STATE_AND_ACTION_PLAN.md`, `SOTA_ROADMAP.md`, `docs/research/*` · **◐ PARTIAL — gap-fixes-batch-1 (batch 8, 2026-06-08)** — 183/269 unambiguous hits converted: repo-own paths → `./`-relative; `aperture-reference-libs/three.js` → `references/three.js`; `.../playcanvas-engine` → `references/engine`. Residual (mechanical, needs upstream URLs — these libs aren't cloned in `references/`): `aperture-reference-libs/Babylon.js` → `https://github.com/BabylonJS/Babylon.js`, `gl-matrix` → `https://github.com/toji/gl-matrix`, `wgpu-matrix` → `https://github.com/greggman/wgpu-matrix`; and `/Users/felixz/Projects/immersive-web-sdk` is a private project with no public equivalent (leave noted).

---

## 4. Outdated / Now-Closed

Gaps the verification found already fixed since the snapshot — with the evidence that closes each. This is the footprint of the recent remediation wave:

- **Asset-backed colliders (convexHull/trimesh/heightfield) never synced; Rapier threw** _(critical)_ — **CLOSED.** `colliders.ts:61-109` cooks all three from `options.colliderGeometryProvider` (`convexHull` :64 with a null-cook guard, `trimesh` :89, `heightfield` :100); `backend.ts` threads the provider and the `destroyBody` fallback is now conditional (try/catch on `RapierColliderSyncError`, re-throws non-sync errors); facade wired at `physics-facade.ts:12,59,117`. **PHY-05/PHY-02 completed 2026-06-06**, with a headless test asserting clean sync.

- **SAB SeqLock zero-copy transport never wired into the default generated worker** _(high — two duplicate entries, both closed)_ — **CLOSED.** `worker/snapshot.ts:46-60` builds shared views from `start.transport`; `writeFrame` is called on every publish in SAB mode (`:152`); host attaches the transport at `create-webgpu-app.ts:161-172`; read side reads via `reader.readLatestFrame`. The "only writer is `examples/sab-cube`" claim is false today. **FEAT-02 completed 2026-06-07.**

- **Specular/diffuse IBL fell back to a flat placeholder; equirect→cube not in the asset pipeline** _(high)_ — **CLOSED.** Equirect is wired on-GPU: `app-environment-resources.ts` accepts `equirectSource`, runs `createEquirectToCubeResource` (real compute pipeline), and feeds the projected cube into diffuse/specular paths "without CPU readback"; specular PMREM is real (`ibl-texture-resource-specular.ts:88,303`, prefiltered:true); diffuse does real cosine-irradiance convolution. Placeholder grey remains only when no env is supplied at all. **FEAT-06 / FEAT-04 / M5-T2 (2026-05-30) done.**

- **Specular IBL prefiltering was a deterministic placeholder; real PMREM/GGX deferred** _(medium)_ — **CLOSED.** A real per-roughness-mip GGX PMREM compute pass runs (`createSpecularIblPmremTextureResource`, `pmrem-compute-pipeline.ts`), engaged whenever a `pmremSource` is present; the app auto-derives the PMREM source from equirect→cube projection. The `specularPrefilteringDeferred`/placeholder diagnostics fire only as a no-source fallback. **FEAT-04 (completed by accepted alternate design).**

- **Generated worker posted full structured-clone snapshots, no transfer list / no pooling** _(high)_ — **PARTIAL→effectively closed for the headline; one residue.** The transferable branch now passes `renderSnapshotTransferList(snapshot)` as the postMessage transfer list (`snapshot.ts:122`; `renderSnapshotTransferList` live at `simulation-worker.ts:276`), and the buffer-pool concern was resolved by **deliberate deletion** after a benchmark showed pooling ~3.9x slower median (FEAT-03). Only residue: per-frame `number[]` extraction allocations → see **AI-30**.

- **Render-snapshot buffer pool / recycle protocol existed but the default worker never recycled** _(medium — two duplicate entries)_ — **CLOSED by deletion.** Repo-wide grep for `createRenderSnapshotBufferPool`/`recycleSnapshotBuffers`/`copyRenderSnapshotIntoBufferLease`/`RenderSnapshotBufferLease`/`bufferLeaseId` returns **zero** hits; `renderSnapshotTransferList` is the live fallback. The dead pool surface no longer exists. **FEAT-03 completed 2026-06-07 (pool not adopted; helpers + cost estimator deleted).**

- **Tier-7 pooled transferable buffer path had zero callers; default worker used no transfer list** _(medium)_ — **CLOSED.** Same FEAT-03/FEAT-02 fix: pool symbols gone, and the default worker now passes the transfer list (or uses the preferred SAB path), eliminating both the zero-caller pool and the no-transfer-list default.

- **`check:examples` was a hand-maintained 244-entry `node --check` chain** _(low)_ — **CLOSED.** `scripts/check-examples.mjs` dynamically enumerates `examplesDir` and spawns `node --check` on every discovered example, failing CI on any syntax error; `package.json:19` delegates to it. The desync footgun is gone. **DOC-01 completed 2026-06-06.**

---

## 5. By-Design / Deferred

Real gaps, intentionally out of scope. Acknowledged so they aren't forgotten; the right-hand note flags which need a **roadmap decision** to pick up.

- **No audio subsystem** _(critical)_ — zero `AudioContext`/`AudioListener`/`PositionalAudio` anywhere; not on any roadmap doc. **Roadmap decision required** to bring spatial audio into the North Star.
- **No accessibility (ARIA / accessibility tree / screen-reader)** _(high)_ — canvas-only UI; no DOM a11y bridge. **Roadmap decision required** (interacts with AI-45/AI-46 once an editable/focus model exists).
- **No networking / replication / multiplayer** _(high — two entries)_ — only the Vite devtools `ws://` bridge exists; sim-authoritative fixed-step is a foundation but no netcode hooks are scoped. **Roadmap decision required.**
- **No internationalization (locale / RTL / message catalog / Intl formatting)** _(medium)_ — only `localeCompare` for sort stability; explicitly an app-framework concern outside the runtime. **Roadmap decision required.**
- **No FBX/OBJ/USD/USDZ/PLY/STL/VRM/gsplat import** _(medium)_ — glTF/GLB 2.0 only; SOTA_ROADMAP:636 explicitly "do NOT add" these. Deferred by roadmap.
- **No Gaussian-splat / 3D-capture rendering** _(low)_ — out of scope per ENGINE_COMPARISON.md:95 and SOTA_ROADMAP:591; enabling LOD/GPU-cull gaps tracked separately. Deferred by roadmap.
- **No WebP/AVIF image support (`EXT_texture_webp/avif` rejected)** _(low)_ — png/jpeg/ktx2 allowlist; SOTA_ROADMAP:636 "do NOT add webp/avif" for M1. Deferred by roadmap.
- **Mesh LOD absent (no LOD component/metric)** _(medium)_ and **No LOD of any kind** _(low)_ — SOTA M8-T8, fully spec'd, gated on M3/M8-T3, not started. Roadmapped future milestone. _(Distinct from AI-24, which is about consuming the already-authored simplified-mesh field.)_
- **No deferred / forward+ (visibility-buffer) render path** _(medium)_ — SOTA M8-T9, a graph-composition capstone gated on M3+M8-T4/T6. Roadmapped.
- **Compute BRDF (DFG) LUT generated but never sampled; analytic Karis DFG used** _(low — two entries)_ — a recorded engineering decision (DECISION 0017; FEAT-04 "Completed by accepted alternate design"): the validated GPU LUT is intentionally unbound. **A new rendering-quality decision is required** to switch to literal LUT sampling.
- **Test backend approximates every collider shape as a sphere** _(medium)_ — accepted property of the `/testing`-gated deterministic backend (plan lines 826-833); real backend is Rapier. _(Pairs with AI-4, which addresses the missing solver / honest flags.)_
- **Dedicated physics-worker (transferable) route is not the default** _(low)_ — PHY-01/PHY-03: simulation-worker physics is the deliberate default; "do not promote without a future explicit decision." **Roadmap decision required** to promote.
- **No on-screen stats/FPS/profiler overlay** _(low)_ — SOTA M11-T1, not-started; diagnostics data is collected but not surfaced as a HUD. Roadmapped (M11 adoption milestone). _(Overlaps AI-83's inspector.)_
- **WebXR is an inert stub** _(high)_ — `xr.active` declared with zero usages; no XR Web API references; ROADMAP Phase 14 defers XR view abstraction/session/stereo/controllers. **DOC-05 added `WEBXR_IMPLEMENTATION_PLAN.md` (planning only).** Roadmapped (Phase 14).

---

## 6. Corrections

Gaps where the adversarial recheck corrected the first-pass verdict or its evidence. Per instructions, the recheckNote is the truth.

- **"Full WebGPU e2e suite is only runnable via a Claude Code SessionStart hook"** _(medium)_ — **verdict corrected: still-open → WRONG.** Git history shows `playwright.config.ts` was committed 2026-05-15 (before the snapshot), runs the whole `./test/e2e` dir under headed real Chrome with `--enable-unsafe-webgpu`, and is wired to `pnpm test:e2e` in `package.json:25`. `playwright.ci.config.ts` also runs the full dir (testIgnore only `dof.spec.ts`), not 4 specs. Only `playwright.local.config.ts` is git-ignored — and it's just the GPU-less SwiftShader convenience wrapper, not the only or documented path. The original claim that the hook is the only way is false. **No action item.**

- **"IBL/environment-map prefiltering falls back to unsupported placeholder textures"** _(medium, partial)_ — **verdict held (partial); evidence corrected.** The first pass cited a nonexistent file `specular.ts:303,546-549,555-564`; those lines actually live in `packages/webgpu/src/lighting/ibl-texture-resource-specular.ts`. Substance holds: PMREM is real and E2E-proven, but the `!prefiltered` fallback (`:550-566`) and the `ibl-resource-descriptor.ts:179-193` "unsupported" placeholder remain for unprepared/no-pmrem-source maps. → **AI-87** _(downgrade stale "deferred" messaging; ensure every authored env map reaches the prefilter path)_, files `ibl-resource-descriptor.ts:179-193`, `ibl-texture-resource-specular.ts:545-567`, `diffuse-ibl-resource-summary.ts:197-215`, P2/M. **Promoted into §3 (Rendering & GPU perf) as a numbered, tracked item on 2026-06-08** — re-verification confirmed PMREM is real and the placeholder/`specularPrefilteringDeferred` fallback persists only for unprepared / no-source maps.

- **"Specular IBL prefiltering is a deterministic placeholder; real PMREM/GGX deferred"** _(medium, outdated-fixed)_ — **verdict held (outdated-fixed); evidence corrected.** The first pass cited `packages/render/src/rendering/app-environment-resources.ts` (which does not exist). The real PMREM/equirect wiring lives at `packages/webgpu/src/app/app-environment-resources.ts` (`specularSourcesForAsset:766`, derived from `equirectProjection:782-786`, threaded via `pmremSources:336`). Implementation is real; gap is closed (listed in §4).

_(No other rows were flagged `corrected`; all remaining verdicts were `confirmed`. Several `confirmed` rows carried minor line-number drift — e.g. `browser/input.ts` listeners extend to :127/:191 not :123; `advanced.ts` step bracketing is :177-188 not :167-187 — immaterial to verdict, and the corrected line refs are reflected in the action items above.)_

---

## 7. Recommended Sequencing — Do Next (re-prioritized 2026-06-08)

The 2026-06-07 list's cheap wins (AI-79, AI-63, AI-48) have **landed** and are dropped here. Remaining open/partial items, ordered by leverage × tractability:

1. ~~**AI-11 — Gate the per-frame GPU drain on a real readback need**~~ **DONE (batch 9, 2026-06-09)** — see the Implementation Log; the occlusion N-frame-latency follow-on remains open.
2. **AI-72 — Cut the first real `v*` release + changelog** _(P1/M)_. Unblocks external adoption; the machinery exists and 14 changesets are staged. No code deps.
3. **AI-66 — Add the dirty-range-aware buffer-upload primitive (`writeBufferSubData`)** _(P3/L)_. The 5-arg `queue.writeBuffer` shape is already declared; this is the enabling primitive for AI-64/AI-65. Bumped early despite P3 because it unblocks the whole upload chain.
4. **AI-19 — Emit a normal G-buffer + thread the live view uniform into post `prepare()`** _(P2/XL)_. Hard prerequisite for AI-20 and a building block toward deferred. Do before AI-20.
5. **AI-20 — Rewrite SSR as general screen-space reflection** _(P2/L)_, using AI-19's real normals + live view uniform. High visual payoff once unblocked; depends on AI-19.
6. **AI-12 — Route forward + legacy paths through the frame graph and read `userPassRegistry`** _(P1/L)_. The public user-pass API currently no-ops on the default render; mirror the existing post-processing node wiring. Pairs with AI-25.
7. **AI-25 — Flip `useFrameGraph` default ON at parity + add a graph-route gating spec** _(P3/S)_. Small residual once AI-12 covers the forward user-pass path. Cheap finisher.
8. **AI-47 — Wheel input event kind + `UiScroll` wheel/drag mapping** _(P1/L)_. Closes a P1 input gap with no dependencies; makes scrollable UI actually function.
9. **AI-64 — Wire `RenderSnapshotUpdateSchedule` + per-entity version into dirty-range transform uploads** _(P2/XL)_. The central perf payoff of the upload chain. Depends on AI-66 (primitive) and AI-13/AI-67 (extraction versioning).
10. **AI-13 — Own and thread a persistent `RenderExtractionCache` per app instance** _(P2/M)_. Foundational for AI-30/AI-67/AI-64 and a measurable per-frame allocation win. Design alongside AI-67's transform-version split.
11. **AI-60 — Dirty-flag transform propagation + incremental spatial index + collapse the double resolve/refresh per `step()`** _(P2/XL; start with the cheap half)_. The root that makes AI-67 and incremental culling tractable; the redundant double resolve/refresh is a near-free win to split out first.
12. **AI-1 — Diagnose primitive-collider non-unit scale (path b first)** _(P1/M)_. Ship the diagnostic-only path first — baking scale into the descriptor would recreate colliders every frame via `colliderKeyFor` (exclude scale from that signature before path a). Closes a P1 silent-desync footgun safely.
13. **AI-71 — Replace the 4 hard-coded e2e specs with a sharded matrix + macOS real-GPU runner** _(P1/L)_. Gates AI-74/AI-75 and is the prerequisite for trustworthy regression coverage.
14. **AI-87 — Populate `pmremSources` for all authored env maps** _(P2/M)_ so the unsupported/deferred placeholder is unreachable (cubemap-direct as well as equirect-derived); remove the stale diagnostics. Self-contained; newly promoted into §3.
